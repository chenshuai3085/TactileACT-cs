# Codex Local Rules

- `工作记录codex.txt` is a local-only research/work log. Do not add, stage, commit, or push it.
- If `工作记录codex.txt` appears as tracked in git, remove it from the index with `git rm --cached 工作记录codex.txt` while keeping the local file.
- Keep experiment conclusions in local notes unless the user explicitly asks to create a tracked research document.
