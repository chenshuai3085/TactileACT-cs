可以。下面我直接给你一版**B 路线：基于 LeRobot-Diffusion 的可执行方案**。我会把它收敛成：

**先做一个“最小可运行版本”**，目标不是一步到位做成 TouchGuide 或 TF-WM，而是先验证三件事：

1. 触觉编码能否稳定接入 DP。
2. 当前触觉是否能学出一个可用的 feasibility / contact score。
3. 短视野 future contact target 是否能从同一套数据里学出来。

这条路线和 TouchGuide 的相似点在于：都让触觉不只做输入，还参与动作评价；TouchGuide 的 CPM 本质上是当前视觉、当前触觉和当前动作的 feasibility scorer，训练上用同一步 observation-action 配对做对比学习。
但我们这里**不把它做成 inference-time classifier guidance**，而是先做成**外接 reranker / auxiliary head**，这样更容易落地，也能为后续 TF-WM 留接口。

------

# 一、先定最终目标：B 路线做成什么样

我建议你把 B 路线定义成：

> **DP + tactile encoder + contact feasibility head + optional future contact head + candidate reranking**

不是直接改 diffusion 采样过程，而是分两阶段：

### Phase B1：最小版

- 训练一个 **视觉 DP baseline**
- 增加 **触觉编码器**
- 增加 **current feasibility head**
- 在推理时对候选 action chunks **重排序**

### Phase B2：增强版

- 在 B1 基础上增加 **future contact head**
- 预测 chunk 执行后 1 到 H 步内的接触趋势
- 用当前 feasibility + future contact score 联合排序

这个设计比直接上 TouchGuide 式 guidance 更稳，因为你不需要一开始就改 denoising / flow matching 采样内环。TouchGuide 的主创新确实是 sampling-time steering，但这一步对工程侵入更大。

------

# 二、工程目录建议

你现在先不要改一堆地方。建议新开一个分支目录，例如：

```text
project_root/
  lerobot_base/                  # 原始或你的LeRobot工程
  tactile_dp/
    configs/
      task_peg_insert.yaml
      task_socket_insert.yaml
      train_dp_vision.yaml
      train_dp_tactile.yaml
      train_feasibility.yaml
      train_future_contact.yaml

    datasets/
      tactile_chunk_dataset.py
      tactile_label_utils.py

    models/
      tactile_encoder.py
      multimodal_obs_encoder.py
      dp_wrapper.py
      action_encoder.py
      feasibility_head.py
      future_contact_head.py
      reranker.py

    trainers/
      train_dp.py
      train_feasibility.py
      train_future_contact.py

    eval/
      eval_policy.py
      eval_rerank.py
      eval_contact_metrics.py

    utils/
      normalization.py
      chunking.py
      logging_utils.py
```

核心原则是：

- **DP 主干尽量少改**
- 新东西全部放 `tactile_dp/`
- 与 LeRobot 的耦合只放在 dataset 和 policy wrapper 两层

------

# 三、数据设计：先别追求全字段，先把最必要的字段跑通

## 3.1 一个样本的最小字段

我建议你先做如下字段：

```python
sample = {
    "rgb": Tensor$T_obs, N_cam, 3, H, W$,
    "tactile_rgb": Tensor$T_obs, N_finger, 3, Ht, Wt$,   # 或 depth / marker_offset 二选一先上
    "proprio": Tensor$T_obs, Dp$,
    "action_chunk": Tensor$H_act, Da$,                   # eef_rel chunk
    "feasibility_label": Tensor$1$,                      # 当前时刻动作是否“接触合理”
    "future_contact_label": Tensor$H_pred, Dc$,          # 可选，B2再上
}
```

其中：

- `T_obs`：建议 1 或 2，先别做长历史
- `H_act`：建议 8 或 16
- `Da`：eef_rel action dim
- `Dc`：先做 3 个量就够
  例如 `$contact, jam, progress$`

## 3.2 动作表示

直接沿用相对末端动作最顺。TouchGuide 论文本身也明确采用 relative ee pose 作为 policy 训练的 action format。

所以这里我建议你用：

$
a_t = $\Delta x,\Delta y,\Delta z,\Delta r_x,\Delta r_y,\Delta r_z,\Delta g$
$

或者你现有更稳定的 eef_rel 版本。

------

# 四、标签怎么来：先自动生成，不要人工标太多

这是最关键的落地点。

## 4.1 feasibility_label 怎么定义

B1 先做一个**弱监督 feasibility label**，不要追求完美真值。

我建议第一版规则：

### 正样本

来自专家演示中的真实 observation-action 配对：

$
(o_t, a_{t:t+H-1}) \rightarrow y=1
$

### 负样本

两种方法混合：

1. **时间错位负样本**
   $
   (o_t, a_{t+\Delta:t+\Delta+H-1}) \rightarrow y=0
   $
2. **加噪声动作负样本**
   $
   (o_t, \tilde a_{t:t+H-1}) \rightarrow y=0
   $

这和 TouchGuide 的 CPM 很接近：它就是把同一步的 observation-action latent 设为正样本，不同步设为负样本，并对动作加噪以适应推理时的 noisy action space。

## 4.2 future_contact_label 怎么定义

B2 再上。第一版只做 3 个：

- `contact`: 未来是否接触增强
- `jam`: 未来是否出现卡住/高阻塞
- `progress`: 是否朝成功插入推进

### 可操作定义

#### contact

由触觉强度阈值或 marker_offset / depth deformation 阈值产生

#### jam

可用以下弱标签：

- 接触强，但末端位移小
- 连续若干步 progress 不增长
- 或 force 上升但插入深度无明显增加

#### progress

如果你有示教轨迹末端位姿或插入深度 proxy，可定义为：

- 到成功终点的相对距离减小
- 或沿任务主轴方向位移增加

第一版宁可粗糙，也不要因为追求“完美标签”停住。

------

# 五、模型结构

------

## 5.1 tactile encoder

先不要同时上 tactile RGB + depth + marker_offset 三模态。
第一版只建议：

- **tactile depth** 或
- **marker_offset**

二选一。
如果你现有 pipeline 最成熟的是触觉 RGB，也可以先用 RGB。

### 推荐结构

```python
tactile_encoder = SmallCNN or ViT-tiny
```

输出：
$
z_t^{tac} \in \mathbb{R}^{D_t}
$

如果双指触觉，就左右指分别编码，再做 concat + MLP。

------

## 5.2 multimodal observation encoder

输入：

- vision embedding (z_t^{vis})
- tactile embedding (z_t^{tac})
- proprio (p_t)

输出：

$
z_t^{obs} = f_{\text{obs}}($z_t^{vis}, z_t^{tac}, p_t$)
$

建议第一版就是：

```python
obs_token = MLP(concat(vis_emb, tac_emb, proprio))
```

先别一上来搞 cross-attention。

------

## 5.3 action encoder

因为 feasibility 是 observation-action 匹配问题，所以你要单独编码 action chunk。

$
z_t^{act} = f_{\text{act}}(a_{t:t+H-1})
$

推荐：

- 1D CNN + MLP
  这和 TouchGuide CPM 的 action encoder 形式接近。

------

## 5.4 feasibility head

第一版最推荐用**对比式匹配**，不是 BCE classifier。

定义：

$
s_t = \langle \hat z_t^{obs}, \hat z_t^{act} \rangle
$

其中两个 embedding 都做 L2 normalize。

这和 TouchGuide 的 feasibility score 完全同型：它就是 observation latent 与 action latent 的 cosine similarity。

训练损失：

$
\mathcal L_{feas} = \frac{1}{2}(\mathcal L_{o\to a} + \mathcal L_{a\to o})
$

这也是 TouchGuide CPM 的核心 contrastive loss。

------

## 5.5 future contact head

这个是 B2 的关键升级。

输入：

- 当前 observation embedding (z_t^{obs})
- candidate action chunk (a_{t:t+H-1})

输出：

$
\hat y_{t}^{future} \in \mathbb{R}^{H_{pred}\times D_c}
$

例如：

$
D_c = 3 \quad \text{for } $contact, jam, progress$
$

第一版用一个小的 temporal MLP / GRU 就够：

```python
future_head(obs_emb, action_chunk) -> future_contact_logits
```

不要把它做成完整 world model。

------

# 六、训练分三步走

------

## Step 1：先训练视觉 DP baseline

目标：先确认你的 task 在 LeRobot-DP 上能跑。

输出：

- `dp_vision.ckpt`

如果这个都不稳，后面不用谈。

------

## Step 2：训练 feasibility model

冻结或半冻结 DP，只用离线数据训练：

- tactile encoder
- multimodal obs encoder
- action encoder
- feasibility head

输出：

- `feasibility.ckpt`

### 训练目标

输入真实观测和真实动作 chunk，学习 observation-action 对齐。

------

## Step 3：训练 future contact head

继续用同一批数据，输入：

- observation
- ground-truth action chunk

监督：

- future contact / jam / progress labels

输出：

- `future_contact.ckpt`

注意：
**B1 只做到 Step 2 就已经能出第一轮实验。**
B2 再做 Step 3。

------

# 七、推理流程

## B1：只用 feasibility reranking

1. DP 采样 (K) 个 candidate chunks
2. feasibility model 对每个 chunk 打分
3. 选 top-1
4. 执行前 1～2 步
5. 重复

## B2：feasibility + future contact 联合评分

最终分数：

$
S = \alpha s_{feas} + \beta s_{future}
$

其中

# $ s_{future}

w_1 \cdot \text{contact_good}

- w_2 \cdot \text{jam_risk}

- w_3 \cdot \text{progress}
  $

------

# 八、关键伪代码

------

## 8.1 数据集切片

```python
class TactileChunkDataset(Dataset):
    def __init__(self, episodes, obs_horizon=1, act_horizon=8, pred_horizon=3):
        self.items = $$
        for ep in episodes:
            T = len(ep$"actions"$)
            for t in range(obs_horizon - 1, T - act_horizon - pred_horizon):
                item = {
                    "rgb": ep$"rgb"$$t - obs_horizon + 1 : t + 1$,
                    "tactile": ep$"tactile"$$t - obs_horizon + 1 : t + 1$,
                    "proprio": ep$"proprio"$$t - obs_horizon + 1 : t + 1$,
                    "action_chunk": ep$"actions"$$t : t + act_horizon$,
                    "future_tactile": ep$"tactile"$$t + 1 : t + 1 + pred_horizon$,
                    "future_state": ep$"proprio"$$t + 1 : t + 1 + pred_horizon$,
                }
                self.items.append(item)

    def __len__(self):
        return len(self.items)

    def __getitem__(self, idx):
        item = self.items$idx$
        item$"feasibility_label"$ = 1
        item$"future_contact_label"$ = build_future_contact_label(
            item$"future_tactile"$, item$"future_state"$
        )
        return item
```

------

## 8.2 负样本构造

```python
def make_negative_action(dataset, idx, mode="time_shift"):
    item = dataset.items$idx$

    if mode == "time_shift":
        j = random.randint(0, len(dataset.items) - 1)
        while j == idx:
            j = random.randint(0, len(dataset.items) - 1)
        neg_action = dataset.items$j$$"action_chunk"$

    elif mode == "noise":
        action = item$"action_chunk"$.copy()
        noise = np.random.normal(scale=0.02, size=action.shape)
        neg_action = action + noise

    else:
        raise ValueError(mode)

    return neg_action
```

------

## 8.3 observation encoder + action encoder + feasibility

```python
class FeasibilityModel(nn.Module):
    def __init__(self, vis_encoder, tac_encoder, proprio_dim, action_dim, emb_dim=256):
        super().__init__()
        self.vis_encoder = vis_encoder
        self.tac_encoder = tac_encoder

        self.obs_mlp = nn.Sequential(
            nn.Linear(vis_encoder.out_dim + tac_encoder.out_dim + proprio_dim, 512),
            nn.ReLU(),
            nn.Linear(512, emb_dim),
        )

        self.act_encoder = nn.Sequential(
            nn.Conv1d(action_dim, 128, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.Conv1d(128, 128, kernel_size=3, padding=1),
            nn.ReLU(),
        )
        self.act_proj = nn.Sequential(
            nn.Linear(128, 256),
            nn.ReLU(),
            nn.Linear(256, emb_dim),
        )

    def encode_obs(self, rgb, tactile, proprio):
        vis_emb = self.vis_encoder(rgb)          # $B, Dv$
        tac_emb = self.tac_encoder(tactile)      # $B, Dt$
        x = torch.cat($vis_emb, tac_emb, proprio$, dim=-1)
        z_obs = self.obs_mlp(x)
        z_obs = F.normalize(z_obs, dim=-1)
        return z_obs

    def encode_action(self, action_chunk):
        # action_chunk: $B, H, Da$
        x = action_chunk.transpose(1, 2)         # $B, Da, H$
        x = self.act_encoder(x)                  # $B, 128, H$
        x = x.mean(dim=-1)                       # $B, 128$
        z_act = self.act_proj(x)
        z_act = F.normalize(z_act, dim=-1)
        return z_act

    def forward(self, rgb, tactile, proprio, action_chunk):
        z_obs = self.encode_obs(rgb, tactile, proprio)
        z_act = self.encode_action(action_chunk)
        score = (z_obs * z_act).sum(dim=-1)      # cosine similarity after normalize
        return score, z_obs, z_act
```

------

## 8.4 feasibility contrastive loss

```python
def contrastive_feasibility_loss(z_obs, z_act, temperature=0.07):
    logits_oa = z_obs @ z_act.T / temperature
    logits_ao = z_act @ z_obs.T / temperature

    labels = torch.arange(z_obs.shape$0$, device=z_obs.device)

    loss_oa = F.cross_entropy(logits_oa, labels)
    loss_ao = F.cross_entropy(logits_ao, labels)

    return 0.5 * (loss_oa + loss_ao)
```

------

## 8.5 future contact head

```python
class FutureContactHead(nn.Module):
    def __init__(self, obs_dim=256, action_dim=7, act_horizon=8, pred_horizon=3, out_dim=3):
        super().__init__()
        self.action_embed = nn.Sequential(
            nn.Conv1d(action_dim, 128, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.Conv1d(128, 128, kernel_size=3, padding=1),
            nn.ReLU(),
        )
        self.gru = nn.GRU(
            input_size=obs_dim + 128,
            hidden_size=256,
            num_layers=1,
            batch_first=True,
        )
        self.head = nn.Sequential(
            nn.Linear(256, 128),
            nn.ReLU(),
            nn.Linear(128, out_dim),   # $contact, jam, progress$
        )
        self.pred_horizon = pred_horizon

    def forward(self, obs_emb, action_chunk):
        # obs_emb: $B, D$
        # action_chunk: $B, H, Da$
        a = action_chunk.transpose(1, 2)             # $B, Da, H$
        a = self.action_embed(a).transpose(1, 2)     # $B, H, 128$

        obs_seq = obs_emb.unsqueeze(1).repeat(1, a.shape$1$, 1)
        x = torch.cat($obs_seq, a$, dim=-1)          # $B, H, D+128$

        h, _ = self.gru(x)
        h = h$:, : self.pred_horizon$                # $B, H_pred, 256$

        logits = self.head(h)                        # $B, H_pred, 3$
        return logits
```

------

## 8.6 联合评分与 reranking

```python
@torch.no_grad()
def rank_action_candidates(
    dp_policy,
    feasibility_model,
    future_contact_head,
    obs,
    num_candidates=16,
    alpha=1.0,
    beta=1.0,
    w_contact=0.5,
    w_jam=1.0,
    w_progress=1.0,
):
    # 1. 从 DP 采样多个候选 chunk
    candidates = dp_policy.sample_multiple(obs, num_candidates=num_candidates)
    # candidates: $K, H, Da$

    rgb = obs$"rgb"$.repeat(num_candidates, 1, 1, 1)
    tactile = obs$"tactile"$.repeat(num_candidates, 1, 1, 1)
    proprio = obs$"proprio"$.repeat(num_candidates, 1)

    feas_score, z_obs, _ = feasibility_model(rgb, tactile, proprio, candidates)

    if future_contact_head is not None:
        fut = future_contact_head(z_obs, candidates)       # $K, H_pred, 3$
        contact = fut$..., 0$.sigmoid().mean(dim=-1)
        jam = fut$..., 1$.sigmoid().mean(dim=-1)
        progress = fut$..., 2$.mean(dim=-1)

        future_score = (
            w_contact * contact
            - w_jam * jam
            + w_progress * progress
        )
    else:
        future_score = torch.zeros_like(feas_score)

    total_score = alpha * feas_score + beta * future_score
    best_idx = total_score.argmax()

    return candidates$best_idx$, {
        "feas_score": feas_score,
        "future_score": future_score,
        "total_score": total_score,
    }
```

------

# 九、训练顺序与时间安排

## 第 1 周：跑通 baseline

- 跑 LeRobot-DP vision-only
- 确认数据读取、动作格式、部署接口

## 第 2 周：接 tactile encoder

- 把 tactile 放进 dataset
- 跑一个简单的 multimodal obs encoder
- 确认 forward 没问题

## 第 3 周：训练 feasibility head

- 做正负样本构造
- 训练对比损失
- 离线验证 top-1 / top-k action matching

## 第 4 周：做 reranking 闭环

- DP 采样多个 candidates
- feasibility rerank
- 真机或离线 replay 测

## 第 5～6 周：future contact head

- 做 `contact/jam/progress` 弱标签
- 加 future head
- 做联合评分

这和你前面上传路线图里“先做简化版、先跑通再优化”的思路是一致的。

------

# 十、最关键的实验设计

你现在别做太多 baseline。先做这四个：

### Baseline 1

**DP-vision**

### Baseline 2

**DP + tactile concat**
只把触觉拼进 policy，不做单独 scorer

### Ours-B1

**DP + feasibility reranking**

### Ours-B2

**DP + feasibility + future contact reranking**

最关键的消融不是“换 backbone”，而是：

> **prediction / feasibility signal 是否真的改变了动作选择**

------



这条线和 **TouchGuide** 太近了。TouchGuide 已经做了三件很强的事：

1. 它不是简单拼接触觉，而是用 **CPM** 学习当前视觉、当前触觉、当前动作的 feasibility score；
2. 它不是只离线打分，而是把这个 score 的梯度直接注入 **DP / π0.5** 的采样过程，做 **inference-time steering**；
3. 它已经报告了显著成功率提升，而且推理开销只小幅增加。

所以如果你的方法只是：

- DP
- tactile encoder
- observation-action score
- reranking / guidance

那审稿人很容易说：

> 这不就是 TouchGuide/CPM 的一个较弱变体，或者一个工程改写版？

这个风险是真实存在的。

------

但是该路线能帮你最快验证四件特别关键的事：

1. 你的 tactile 字段到底可不可学；
2. `contact / jam / progress` 这种弱标签能不能自动构出来；
3. 短 horizon 的 action-conditioned contact prediction 是否稳定；
4. 这些模块以后能不能迁到 world model 分支里。

所以其价值不是“它本身一定能发顶会”，而是：

> **它是你最低风险拿到正反馈、并为后续 TF-WM 积累模块的路线。**



### 升级 1：从“当前 feasibility”升级到“未来接触演化”

这是最重要的。

TouchGuide 的 CPM 主要是当前时刻的动作可行性打分：

[
s(V_t, T_t, A_t)
$

而如果你只是做这个，再加一点 reranking，增量不够大。

你至少要走到：

[
p(c_{t+1:t+H}\mid o_t, a_{t:t+H-1})
$

也就是显式预测未来的：

- contact onset
- jam/slip
- progress

这样你才能把故事从“当前打分器”抬升到“future contact prediction”。

### 升级 2：从“外挂 reranker”升级到“结构性融合”

TouchGuide 已经做到了 sampling-time steering。
如果你只是外接 top-k reranking，通常会显得更弱。

所以 B 路线如果要变强，最好后面至少做一个：

- 把 future contact score 进入 diffusion 采样内环
  或
- 做 learned contact critic，和 policy 联合训练
  或
- 做 uncertainty-aware candidate selection

否则审稿人会觉得你只是“后处理”。

### 升级 3：从“policy trick”升级到“world-model / dynamics 贡献”

这正是你后来真正想做的方向。

VT-WM 已经证明了：
把 touch 加进世界模型后，可以显著提升 object permanence、causal compliance 和 contact-rich task 的 real-robot planning success。论文报告了平均约 33% 的 Fréchet 距离下降，以及接触密集任务上最高约 35% 的成功率提升。

所以如果你最终想要的是一个更“像论文”的强故事，其实更自然的是：

> **future contact supervision 能否提升 visuo-tactile world model 的 dynamics quality**

这个故事比 “DP + reranker” 更大，也更容易和 TouchGuide 拉开。

------

### 最终加强版

升级成：

- DP prior
- 显式 future contact prediction
- 不是只做当前 feasibility
- 并且把 prediction 进入 sampling / selection 的核心决策
- 再做漂亮的行为级指标和消融

那它**有机会成为一篇偏 policy / planning 的论文**。


### 后续

- visuo-tactile latent dynamics
- tactile foresight supervision
- 评估 rollout fidelity / causal compliance / downstream planning

VT-WM 重点是 **touch as grounding input**；
你可以做成 **touch as foresight supervision**。