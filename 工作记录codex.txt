2026-06-25
- 真机部署日志新增完整时间戳字段，用于分析 DP/PTG server-client 串行推理流程中的真实 episode 时间、推理步、执行步和控制周期耗时。
- 修改 for_show_xiaomi/server_rollout_logger.py：server 侧 force_trace.csv/npz/metadata.json 新增 wall_time_iso、episode_start/end_wall_time、episode_duration_s、server_obs_recv_wall_time、server_obs_recv_episode_t、server_obs_interval_ms、server_is_replan_step、server_chunk_index、server_chunk_action_index、server_replan_time_ms、server_step_time_ms、server_action_enqueue_wall_time；同时接收并保存 client obs 中携带的上一真实控制步 client_prev_* timing 字段。
- 修改 for_show_xiaomi/ws_client.py：client 侧 force_trace.csv/npz/metadata.json 新增 wall_time_iso、client_step_start/end_wall_time、client_obs_after_action_wall_time、client_step_episode_t、client_send_obs_time_ms、client_wait_action_time_ms、client_env_step_time_ms、client_step_pre_sleep_time_ms、client_control_sleep_ms、client_step_time_ms；并将上一控制步 timing 随下一帧 obs 发给 server。
- 修改 for_show_xiaomi/serve_board_dp_foresight_guided.py：board latent-energy guided/baseline serving 返回并保存 server timing，记录每步是否重新 DP 推理、action chunk 索引、chunk 内 action 索引、重推理耗时和 server 处理耗时。
- Review 后补充 for_show_xiaomi/ws_server.py：server socket 线程在真正收到 obs 时写入 _server_transport_recv_wall_time/_server_transport_recv_perf，guided server 优先用该时间作为 server_obs_recv_wall_time，并额外记录 server_loop_obs_start_wall_time，避免主推理循环延迟污染 obs 接收时间。
- 分析 20260625 nfe50 guided 三条真机擦黑板轨迹时发现 force_trace.csv 均为 300 行，metadata.json 显示 steps=300、stop_reason=max_timesteps；结合用户确认 client 端 max_steps=2000，确定截断来自 server 端 serve_board_dp_foresight_guided.py 的默认 --max_timesteps=300。
- 修改 for_show_xiaomi/guide_forshow.sh：所有擦黑板 latent-energy baseline/guided server 命令显式加入 --max_timesteps 2000，并在注释中说明 client 已经 2000 时只需改 server，避免后续真机长轨迹复测被 300 个 high-level action step 截断。
- 修改 for_show_xiaomi/serve_board_dp_foresight_guided.py：server_metadata 新增 max_timesteps 字段，之后每条 server-side metadata.json 可直接看到该 episode 的 server 上限，不再需要从 stop_reason 和代码默认值反推。
- 新增 for_show_xiaomi/merge_board_rollout_segments.py：把旧真机擦黑板 server-side segment 按时间连续性与 stop_reason 合并为真实 client trial；合并规则为同一 root/group 内按开始时间排序，前一段 stop_reason=max_timesteps 且相邻 gap<=2s 时继续合并，遇到 client_disconnected 结束真实 trial。
- 按用户要求只合并三组旧 guided 数据：oh4_dynamic32768_nfe30_h16_guided_g10_s001、oh4_dynamic32768_nfe50_h16_guided_g20_s001、oh4_dynamic32768_nfe100_h16_guided_g30_s001；输出到 /home/chenshuai/Project/output/board_force_rollouts_merged_trials/selected_nfe_guided，共 27 个 server segment 合并为 8 条 real_trial，并为每条生成 force_trace_merged.csv、force_trace_merged.png、summary.json 与总 summary markdown/json。
- 新增 for_show_xiaomi/analyze_board_success_force_smoothness.py：针对用户指定的三条成功擦黑板 real_trial 做 left Fz 平稳性分析，明确不使用坏掉的 right force 传感器；结合 left_fz、left_marker_mag_mean、eef_z 推断接触/擦拭段，并计算 left Fz std、相邻 |dFz|、短时残差波动、action step L2 平滑度。输出到 /home/chenshuai/Project/output/board_success_force_smoothness_leftfz，包含每条轨迹的 analysis_timeseries.csv、left_fz_action_smoothness.png、summary.json 和三轨迹对比图/总表。当前三条成功轨迹平稳性排序为 nfe50_trial002 最稳，其次 nfe30_trial002，nfe50_trial000 波动最大。
- 更新 for_show_xiaomi/analyze_board_success_force_smoothness.py 可视化：三条成功轨迹单图使用统一坐标轴，left Fz 固定为 0~25N、刻度 5N；短时波动 0~3、action delta 0~3、left marker 0~15、eef_z 0.12~0.24，便于横向直接比较。修复单图重画循环中 name 变量复用导致只覆盖最后一张图的问题，并重新生成全部 PNG。

2026-06-26
- 按用户要求基于 /media/chenshuai/EXTERNAL_USB/pih_dataset/260615_v8l_card/success 的 50 条刷卡 success HDF5 生成 100 条刷卡正样本 EEF 轨迹。先统计 success：平均长度约 428.7 steps、21.4s，起点在 reset 附近，接触/刷卡段 Z 约 166~171mm，主运动为 X 从约 379mm 刷到 187mm。
- 修改 data_collection/generate_trajectories.py：新增 card task 和 CardTrajectoryGenerator，读取 actions/eef_abs（缺失时回退 observations/proprio_eef），构建 success 平均模板；欧拉角先 unwrap 防止 rx 在 +/-pi 附近平均错误，保存前 wrap 回 [-pi, pi]；生成时从 success 残差采样并叠加低频平滑扰动，保留 reset 起点，并加入 48mm/s 保守速度上限，必要时自动重采样拉长轨迹。
- 生成输出到 outputs/card_pos_trajectory_20260626：100 个 card_pos_*.npy，前三条带 PNG 预览，并写入 README.md 与 summary.json。最终统计：长度 354~569 steps、平均 427.01 steps；最低 Z 165.627mm，最高 Z 229.459mm；最大速度 mean 40.054mm/s、p95 46.299mm/s、max 47.951mm/s，无样本超过 auto_replay dry-run 的 50mm/s 提示线。
- 使用 data_collection/auto_replay.py --dry_run 抽查 card_pos_000.npy 和修正前速度最高的 card_pos_015.npy，起点距 RESET 0.2mm、最低 Z 大于 166mm、最大速度分别 46.1/47.8mm/s，检查项均为通过。
- 根据用户反馈“轨迹要能做刷卡任务、步数和路线要和本来的差不多”，新增独立脚本 data_collection/generate_card_success_keepsteps.py。该脚本不再使用模板平均、不做时间拉长/重采样变长；直接读取每条 success 的 actions/eef_abs，保持原始步数和完整路线骨架，只加入极小低频扰动并锁住起末段。
- 生成新版刷卡正样本到 outputs/card_pos_keepsteps_20260626：100 条 card_pos_*.npy，50 条原始 success 每条使用两次；每条输出步数严格等于对应 source，步数范围 344..569。全量验证 length_ok=True，平均 XYZ 偏差 0.134mm，p95 均值 0.312mm，最大偏差 0.958mm。速度最大值 84.8mm/s 来自原始 success 本身，为保持原步数未做时间拉长。
- 撤销前一次错误集成：已从 origin/upstream 的 tacfore 分支追加 aa5b02e，删除误加入 git 的 data_collection/__pycache__/simple_record_pih_v8j(1).py、robotiq_gripper_server_no_ros .py 和错误工作记录条目；本地两个脚本恢复为 /home/chenshuai/Downloads 下原始副本后重新修改。
- 正确修改仅限两个用户指定脚本：data_collection/__pycache__/simple_record_pih_v8j(1).py 与 data_collection/__pycache__/robotiq_gripper_server_no_ros .py。数采脚本本地加载夹爪脚本，初始化 Robotiq 串口夹爪，Sigma7 get_action() 的 buttons 默认取第 0 个数值作为按钮幅度并映射到 0~235；夹爪 move 在后台线程节流发送，避免阻塞 20Hz 主控制循环。
- 数采开始从 Sigma7 按钮改为键盘 S：默认关闭 Sigma7 按钮遥操作门控和 END_ON_TELEOP_DISABLE，按 S 后才开始 warmup/写数据，q 仍结束 episode；保留 --teleop-enable-with-sigma7-button true 可恢复旧按钮门控。
- 数据格式：启用夹爪时 observations/proprio_joint 与 actions/joint_abs 从 7 维扩为 8 维，最后一维为夹爪位置；同时保存 observations/gripper_pos 与 actions/gripper_pos 方便检查。夹爪脚本 move() 限幅从 0~255 改为 0~235，符合用户给定完全打开/关闭范围。已用 AST 语法检查两个脚本通过。

2026-06-29
- 针对 260609/260610 擦黑板 DP ckpt 真机部署动作过慢的问题，按讨论采用 temporal_stride=3 重新训练方案；不删除原始 HDF5 数据，而是在训练 Dataset 内按 3 帧间隔取历史观测/触觉历史/未来动作，保证实验可复现且不破坏原始数据。
- 修改 diffusion/train_dp_tac_concat.py：新增 --temporal_stride 参数，默认 1 保持旧行为；stride=3 时 obs_horizon=4 对应 O(t-9),O(t-6),O(t-3),O(t)，tac_history=8 对应每 3 帧一帧的触觉历史，pred_horizon=16 对应 A(t),A(t+3),...,A(t+45)。
- 新增 scripts/train/train_dp_tac_concat_board_260609_260610_obs4_ph16_temporalstride3_dynamic32768_e1000.sh：沿用原数据集、Board VAE、rawimg200x266、obs_horizon=4、pred_horizon=16、dynamic_train_windows=32768、epochs=1000，但输出目录改为 temporalstride3，避免覆盖 dense baseline。
- 修改 for_show_xiaomi/serve_dp_policy.py 与 for_show_xiaomi/serve_board_dp_foresight_guided.py：部署端从 ckpt config.json 自动读取 temporal_stride，并按 stride=3 构建 DP obs_cond 与触觉历史缓存，避免训练/部署历史帧时间尺度不一致；metadata 同步记录 temporal_stride。
- 已验证：py_compile 通过；bash -n 新训练脚本通过；临时 HDF5 smoke test 验证 temporal_stride=3 时 qpos 历史索引为 [0,3,6,9]，action 标签索引为 [9,12,15,18]。
- 按用户要求给纯视觉官方对齐版 DP 增加跳步训练：修改 diffusion/train_dp_official.py，新增 --temporal_stride 参数，默认 1 保持原官方 dense 行为；stride=3 时 obs_horizon=2 对应 O(t-3),O(t)，pred_horizon=16 对应 A(t),A(t+3),...,A(t+45)，用于训练更快的纯视觉 board wiping baseline。
- 新增 scripts/train/train_dp_official_board_260609_260610_ph16_oh2_temporalstride3_e600_server.sh，服务器路径直接使用 /root/vtm 代码和 /mnt/robot-bionic-control-data/chenshuai18 下的 260609/260610 数据，输出到 tactileact_output/dp_official_vision_board_260609_260610_ph16_oh2_temporalstride3_e600。
- 修改 data_collection/__pycache__/simple_record_pih_v8j(1).py 的 Robotiq 夹爪脚本加载路径：优先兼容真机原始目录 realman_env/robot_servers/robotiq_gripper_server_no_ros .py，找不到时再回退到当前工作目录和脚本同目录，避免把手动夹爪脚本复制到采集脚本同目录或覆盖真机已有文件。
- 本地持续监控 temporal_stride=3 的 260609/260610 DP 训练，输出目录为 /media/chenshuai/SANDISK ELE/tactileact_output/dp_tac_concat_board_260609_260610_left_boardvae_rawimg200x266_ph16_oh4_temporalstride3_dynamic32768_e1000。验证集在 Ep70 达到当前 best val=0.005134；随后 Ep75 val=0.005694、Ep80 val=0.005805、Ep85 val=0.005816，连续 3 次 validation 未刷新且明显高于 best。结论：Ep70 后已进入 plateau/回升风险，继续跑满 1000 epoch 的边际收益很低，当前优先保留 Ep70 best ckpt 作为候选。
- 后续继续监控到 Ep90：train=0.004732 但 val=0.006190，已经连续 4 次 validation 未刷新且比 Ep85 进一步变差，说明训练 loss 下降没有转化为验证集改善。当前判断更偏向 Ep70 后过拟合/验证分布不再受益；如果不做新实验配置，继续跑当前 run 的收益很低。
- 按用户要求在验证集持续变差后提前停止本地 temporal_stride=3 训练。停止前最后完整验证点为 Ep130：train=0.003642、val=0.007890，已连续 12 次 validation 未刷新；当前 best 仍为 Ep70 val=0.005134。已停止 tmux session dp_stride3_260609 和 dp_val_plateau_monitor，GPU 显存从训练时约 23GB 释放到约 1.7GB。
- 针对 temporal_stride=3 DP 重新训练匹配的 foresight 和 latent-energy 引导 scorer。修改 TFAC_V5/dataset.py：ForesightEpisodicDataset 新增 temporal_stride，future tactile/qpos/action chunk 改为 t+offset+k*stride，支持 future_offset=0；保留 TactileVAE 每个 latent 的连续 8 帧编码窗口，不改变 latent 定义。修改 TFAC_V5/pretrain_latent_foresight_multistep.py 读取并打印 temporal_stride。
- 修改 board latent-energy scorer 数据链路：TFAC_V5/board_chunk_energy/dataset.py 的 ChunkDatasetConfig/ChunkIndexRow 新增 temporal_stride，窗口起点 stride 仍表示窗口起点间隔，temporal_stride 才表示窗口内部 t,t+3,...；TFAC_V5/board_latent_energy/dataset.py 与 vae_utils.py 按 row.temporal_stride 读取 action 和 marker latent；train.py/runtime.py 在 checkpoint 中保存并读取 temporal_stride，部署脚本 serve_board_dp_foresight_guided.py 启动时检查 DP/Foresight/scorer stride 是否一致。
- 新增 TFAC_V5/config_pretrain_foresight_board_multistep16_boardvae_marker_only_temporalstride3.json 与 scripts/train/train_foresight_board_multistep16_temporalstride3.sh。训练配置为 marker-only board VAE、chunk/predict horizon=16、future_offset=0、action_offset=0、temporal_stride=3，即 obs[t] + qpos/action[t,t+3,...,t+45] -> tactile[t,t+3,...,t+45]，部署时使用 alignment=none。
- 新增 scripts/train/train_board_latent_energy_temporalstride3.sh，训练 stride=3 latent-energy scorer。输出 /home/chenshuai/Project/output/board_latent_energy/ce_margin_temporalstride3_e40，best checkpoint 为 board_latent_energy_best.pt；manifest 中 row.temporal_stride=3，最终 val macro_f1=1.0、expert_vs_negative_auroc=1.0、expert_margin_auroc=1.0、expert_margin_gap_mean≈25.31。
- 完成 stride=3 foresight 训练，输出 /home/chenshuai/Project/output/foresight_ckpt/latent_foresight_board_260609_260610_multistep16_boardvae_marker_only_temporalstride3_e100_bs16_preload。训练 100 epoch，best 为 epoch 94，val total loss=0.1876，best checkpoint 为 foresight_best.ckpt。后期仍刷新 best，因此跑满 100 epoch，而不是在 55/72 附近提前停止。
- 联调 smoke：使用 DP /media/chenshuai/SANDISK ELE/tactileact_output/dp_tac_concat_board_260609_260610_left_boardvae_rawimg200x266_ph16_oh4_temporalstride3_dynamic32768_e1000/dp_best.pth，新 foresight 和新 scorer，命令中 --alignment none、num_inference_steps=8、guidance_steps=2，dry_run_dataset_smoke_pass=true，grad_finite_mean=1.0。结论：stride=3 DP + stride=3 foresight + stride=3 latent-energy scorer 的部署链路 shape/梯度/配置一致性通过。
- 2026-06-30：检查 /media/chenshuai/EXTERNAL_USB/pih_dataset/260625_v8j_caheiban/peg_in_hole_0625 数据，共 80 条 episode，全部可读，总帧数 55438；单条长度 min=503、median=680、mean≈693、max=914。pred_horizon=16 时 temporal_stride=1 的 required_span=16，总可用窗口 54238；temporal_stride=3 的 required_span=46，总可用窗口 51838；temporal_stride=6 的 required_span=91，总可用窗口 48238。按用户决定先不跳步，新增官方纯视觉 DP 的 val split 支持，并新增脚本 scripts/train/train_dp_official_pih_260625_v8j_caheiban_ph16_oh2_stride1_val5_e600.sh，使用 global+wrist、obs_horizon=2、pred_horizon=16、temporal_stride=1、val_ratio=0.05、e600 配置，输出到 /media/chenshuai/EXTERNAL_USB/pih_output/dp_official_vision_pih_260625_v8j_caheiban_ph16_oh2_stride1_val5_e600。

2026-06-30
- 按用户要求修改 /home/chenshuai/Downloads/simple_record_pih_v8l_forceviz(2).py 的跟随模式：将两处实际发送命令 self.arm.rm_movep_canfd(p.tolist(), 0) 改为 self.arm.rm_movep_canfd(p.tolist(), True)，注释同步从 follow=0 低跟随改为 follow=True 高跟随。该脚本位于 Downloads，不在当前 git 仓库中，因此仓库内仅记录本次操作。
- 按用户要求将 paper 目录下旧 CoRL/TacScore 论文稿件改写为 IEEE RA-L 初投稿 LaTeX 稿。调研确认 RA-L 初投稿/返修使用 RAS `ieeeconf` conference 模板，接收终稿再切 IEEE journal 样式；因此新增 `paper/ieeeconf.cls`、`paper/IEEEtran.bst`、`paper/IEEEabrv.bib`，并在 `paper/README_RAL.md` 记录模板来源与当前写作假设。
- 重写 `paper/main.tex`：从原先“TacScore 候选动作 reranking”主线改为当前“base policy + multi-step tactile foresight + TacQuality score guidance”的 Proactive Tactile Guidance (PTG) 主线；任务覆盖插插座、擦黑板、擦花瓶、刷卡和夹薯片/potato-chip grasping；真实结果未编造，表格和图保留为明确 placeholder，并区分离线 scorer/foresight 证据与待补的 paired real rollout 结论。
- 更新 `paper/references.bib` 顶部注释为 PTG RA-L draft；生成新的 RA-L 源码包 `paper/TacScore_RAL2026_source.zip`，保留原始 `paper/TacScore_CoRL2026.zip` 不覆盖。
- 本地监控 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260625_v8j_caheiban/peg_in_hole_0625` 的官方纯视觉 DP stride=1 训练。训练集 76 条 episode/51437 windows，验证集 4 条 episode/2801 windows；best val 在 Ep25 达到 0.006098。随后 val 长时间未刷新：Ep50 val=0.006228、Ep55 val=0.006230、Ep60 val=0.007081、Ep65 val=0.007077、Ep70 val=0.007954，而 train loss 仍下降到 Ep70 train=0.004132。按用户“变差就提前停止”的要求，已停止 tmux 训练，当前部署/评估优先使用输出目录中的 `dp_best.pth`，不建议使用 Ep70/topk train ckpt。
- 检查 `/media/chenshuai/czy_data22/pih_dataset/260630_v8j_huaping/peg_in_hole_0630` 数据：根目录 `episode_1.hdf5` 到 `episode_100.hdf5` 齐全且全部可读；显式排除子目录 `无夹取位置变化`，当前官方 DP 脚本只读取 dataset_dir 根目录下的 episode 文件，不递归读取该子目录。100 条数据总帧数 43742，长度 min=359、median=438.5、mean=437.42、max=518。按用户要求新增训练脚本 `scripts/train/train_dp_official_pih_260630_huaping_ph16_oh2_stride1_val5_e600.sh`：参数沿用上一次官方纯视觉 PIH 训练，global+wrist、pred_horizon=16、obs_horizon=2、temporal_stride=1、val_ratio=0.05、epochs=600，持续保存 `dp_best.pth`。已启动 tmux `dp_official_pih260630_huaping_stride1_val5_e600`，输出到 `/media/chenshuai/czy_data22/pih_output/dp_official_vision_pih_260630_huaping_ph16_oh2_stride1_val5_e600`；首轮 `Ep1 train=0.171951 val=0.040387`。根据用户后续要求，监控策略调整为至少训练到 Ep200；若 Ep200-Ep300 之间 val 基本不再刷新且趋势停滞/变差，则可在该区间停止，保留 best ckpt；若仍持续刷新 best，则继续训练。
- 为“下午数据” `/media/chenshuai/EXTERNAL_USB/pih_dataset/260625_v8j_caheiban/peg_in_hole_0625` 准备纯视觉 DP rerun 脚本 `scripts/train/train_dp_official_pih_260625_v8j_caheiban_ph16_oh2_stride1_val5_e600_rerun_min200.sh`，输出到不覆盖旧结果的 `..._rerun_min200` 目录，参数同样为 global+wrist、PH16/OH2、temporal_stride=1、val_ratio=0.05、epochs=600，监控策略至少跑到 Ep200。当前机器上 `/media/chenshuai/EXTERNAL_USB` 未挂载，`SANDISK ELE`、`czy_data21`、`czy_data2` 下也未找到 `peg_in_hole_0625`，因此该 rerun 等对应外盘/路径恢复后再启动。
- 新增并启动队列监控脚本 `scripts/train/monitor_pih_dp_queue_260630_then_260625.py`，tmux session 为 `pih_dp_queue_monitor_260630_then_260625`。监控策略：当前 260630_huaping 至少跑到 Ep200；Ep200 之后若 best val 已经 50 个 epoch 未刷新则发送 Ctrl-C 停止并保留 `dp_best.pth`；随后检测 260625 数据路径是否挂载，存在则自动启动 rerun，不存在则每 5 分钟等待。低磁盘空间时仅清理旧 `dp_epoch*.pth`，不删除 `dp_best.pth` 或 topk。

2026-06-30
- 按用户要求调研两个月内可做的触觉/视触觉 world model 研究方向，整理文档 `research/2026-06-30_两个月视触觉world_model调研与选题建议.md`。结论：不建议两个月内做大型 VT-WM/OmniVTA 式视频生成 world model；推荐做轻量级 action-conditioned tactile/force consequence model，以当前 vision/proprio/tactile + action chunk 预测 future tactile latent/marker/force/contact proxies，再接 TacQuality/force-quality energy 做 bounded DP guidance。主任务建议先定擦黑板，因为 force band、smoothness、contact gate 和现有真实 force trace 证据最完整；插孔可作为后续第二任务。

2026-07-01
- 按用户要求在 /home/chenshuai/Project/mi_pih/realman-infra 中不改原有采集文件，新增连续夹爪专用文件：realman_env/spacemouse/sigma7_expert_abs_gripper.py 与 simple_record_pih_v8j_gripper.py。
- 新 Sigma7 expert 从原 sigma7_expert_abs.py 复制而来，去掉 dhd.emulateButton(True) 对两指夹的二值化占用，新增 dhd.getGripperGap 连续读取，归一化为 get_gripper(): 0=闭合、1=张开；原 button0 仍保留给真实物理按钮/teleop 逻辑，避免捏两指夹导致 teleop 反复开关。
- 新采集脚本只导入 sigma7_expert_abs_gripper，并优先用 env.spacemouse.get_gripper() 控制 Robotiq；映射方向为 Sigma7 0=闭合/1=张开 -> Robotiq 235=闭合/0=打开，同时保留 buttons 控制回退。原 simple_record_pih_v8j(1).py、sigma7_expert_abs.py 未修改。
- 验证：PYTHONPYCACHEPREFIX=/tmp/mi_pih_pycache python -m py_compile 两个新脚本通过。mi_pih 仓库已有用户未提交改动和未跟踪文件，本次只提交两个新增文件，commit 为 e90fd9f。

2026-07-02
- 修复 `data_collection/__pycache__/simple_record_pih_v8j(1).py` 中力/夹爪可视化没有显示的问题：原脚本 `--visualize-ft` 依赖缺失的 `visualize_ft.py`，导致只有两个相机窗口，没有六维力曲线窗口。
- 在 v8j 数采脚本内置 `ForceGripperVisualizer`，使用 OpenCV 单独显示 `Fx/Fy/Fz/Tx/Ty/Tz` 实时曲线，并在同一窗口显示 Robotiq 夹爪命令位置条（0=open，235=close）。每个控制 step 会 push 当前 `obs['ft']` 与 `current_gripper_pos`。
- 调整命令行行为：`--visualize` 现在默认同时启动相机/触觉窗口和六维力/夹爪窗口；`--visualize-ft` 仍可单独打开力/夹爪窗口；新增 `--no-visualize-ft` 用于只看相机窗口。已用 `python -m py_compile` 与 `git diff --check` 验证通过。

2026-07-03
- 排查近期 PIH 官方纯视觉 DP 训练中 validation loss 早期最优、后期回升的问题。结论：更像是少量 episode、小验证集、强重叠滑窗和 306M 级别视觉 DP 容量造成的过拟合/验证噪声，不是当前发现的采样窗口代码错误。已整理到 `research/2026-07-03_PIH_DP过拟合与刷卡训练记录.md`。
- 针对刷卡数据先采用保守 success-only 纯视觉 DP baseline：新增 `scripts/train/train_dp_official_card_260629_success_ph16_oh2_stride1_val15_e600.sh`，只读取 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/peg_in_hole_0629/success`，不混入 `bounce_hengxiang`、`bounce_jiaozhun` 或 260701 的 `bounce_*` 数据；参数为 global+wrist、PH16/OH2、n_action_steps=8、temporal_stride=1、val_ratio=0.15、epochs=600，输出到 `/media/chenshuai/EXTERNAL_USB/pih_output/dp_official_vision_card_260629_success_ph16_oh2_stride1_val15_e600`。
- 明确记录：纯视觉 DP 不需要 TactileVAE；如果后续做 tactile+vision DP、foresight 或引导，刷卡任务应训练 card-specific TactileVAE 后再训练对应 policy/foresight/scorer。

2026-07-05
- 按用户要求准备新任务 task-local TactileVAE 训练。新增 `scripts/pretrain/run_tactile_vae_huaping_260630_left.sh`：只使用 `/media/chenshuai/czy_data22/pih_dataset/260630_v8j_huaping/peg_in_hole_0630` 根目录 100 条 episode，显式排除 `无夹取位置变化`，输出 `/media/chenshuai/czy_data22/pih_output/tactile_vae_huaping_260630_left_tw8_ld16_s2_e150`。
- 新增 `scripts/pretrain/run_tactile_vae_card_260615_260626_260629_260701_left.sh`：使用 260701 card bounce、260629 card bounce/success、260626 replay card、260615 card bounce/success 训练 card-specific marker TactileVAE，显式排除 `260629_v8j_card/260630_v8j_huaping` 这个嵌套的非刷卡目录，输出 `/media/chenshuai/EXTERNAL_USB/pih_output/tactile_vae_card_260615_260626_260629_260701_left_tw8_ld16_s2_e150`。
- 检查夹薯片 260702/260703 HDF5 后发现关键阻塞：数据只有 `observations/tac/left/img` 和 `observations/tac/right/img`，没有现有 TactileVAE/DP concat 链路需要的 `observations/tac/{left,right}/marker_offset`。因此当前不能直接训练“和之前一样”的 marker TactileVAE + frozen concat DP；需要用户确认是补算/重采 marker_offset，还是改走 raw tactile image DP/新 tactile image VAE。详细记录在 `research/2026-07-05_任务TactileVAE训练计划与夹薯片数据阻塞.md`。
- Huaping marker TactileVAE 已完成训练，输出 `/media/chenshuai/czy_data22/pih_output/tactile_vae_huaping_260630_left_tw8_ld16_s2_e150/best_tactile_vae.pt`，best val loss=0.025630，final cosine=0.979，final angular error=6.5°。
- 按用户“跳步后任务步数约 200-300”的要求，为 huaping/card 有触觉 DP 选择 `temporal_stride=2`。新增 huaping DP 脚本 `scripts/train/train_dp_tac_concat_huaping_260630_left_huapingvae_ph16_oh2_stride2_dynamic32768_e600.sh`；新增 card positive-only DP 脚本 `scripts/train/train_dp_tac_concat_card_positive_left_cardvae_ph16_oh2_stride2_dynamic32768_e600.sh`；新增队列脚本 `scripts/train/run_huaping_card_tactile_dp_queue.sh`，等待 card VAE 完成后顺序训练 huaping DP 与 card DP，避免单 GPU 同时跑两个大 DP。
- 训练状态复查：当前没有 tmux 训练进程，GPU 空闲；card TactileVAE 在 Ep20 后写 `/media/chenshuai/EXTERNAL_USB/pih_output/tactile_vae_card_260615_260626_260629_260701_left_tw8_ld16_s2_e150/train_history.json` 时报 `FileNotFoundError` 中断，结合 `mount/findmnt` 显示 `EXTERNAL_USB`、`czy_data22`、`SANDISK ELE` 都是 `ro` 只读挂载，判断为外接盘掉盘/只读挂载导致输出目录不可写。按用户最新要求更新 DP 数据源：card tactile DP 只用 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260626_replay_card/card_pos_keepsteps_20260626` 与 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/peg_in_hole_0629/success`；huaping tactile DP 改用 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/260630_v8j_huaping/peg_in_hole_0630` 根目录 100 条 episode，排除子目录 `无变化`。VAE 仍按任务使用全量可用 marker tactile 数据；DP 继续采用 `temporal_stride=2`，目标为下采样后约 200-300 步。
- 按用户“外接盘只读数据，ckpt/cache 写内置盘”的要求，将 huaping/card TactileVAE、huaping/card tactile DP、DP 队列脚本的默认 `OUTPUT_ROOT` 统一改为 `/home/chenshuai/Project/output/pih_tactile`。新增完整单 GPU 队列脚本 `scripts/train/run_huaping_card_tactile_full_queue_internal.sh`：顺序执行 huaping TactileVAE、card TactileVAE、huaping tactile DP、card tactile DP；外接盘仅作为只读数据源，所有日志、VAE ckpt、DP ckpt 和 image cache 均写入内置 NVMe。数据复查：huaping 指定路径 100 条 episode，card replay 53 条，card success 20 条，左手 `marker_offset` 均存在且无坏文件；`temporal_stride=2` 后平均步数约 huaping 219、card replay 216、card success 230，符合用户希望的 200-300 步范围。
