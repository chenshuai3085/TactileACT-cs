2026-07-28
- 按用户要求先画 ForeTac overview 架构图。新增 `paper/plot_foretac_overview.py`，生成 `paper/figures/foretac_overview.png` 和 `paper/figures/foretac_overview.pdf`。图中采用横向 inference 主链：Observation -> Base Policy -> Tactile Foresight -> Contact Energy -> Trust-region Guide -> Robot Execution；红色虚线表示 scorer/guide 对 action 的 late-denoising 梯度回传；底部灰色带表示训练信号，包括 demonstration action chunks、future TacVAE latent targets、semantic contact labels 和 trust-region constraints。本次只新增图和可复现绘图脚本，暂未替换 `paper/main.tex` 中原 teaser/architecture 图，等待用户确认风格。

2026-07-27
- 按用户要求验证 board latent energy scorer 的 Preference-order accuracy。使用可找到的 rollout smoke HDF5 数据 `/home/chenshuai/Project/output/real_rollout_quality_gate_smoke_input`，标签来自 `/home/chenshuai/Project/output/real_rollout_quality_gate_smoke/board_smoke_metadata_min2/real_rollout_episode_metrics.csv` 的 `success_attr`；该子集共 4 条 episode，3 正 1 负。用 checkpoint `/home/chenshuai/Project/output/board_latent_energy/ce_margin_e10/board_latent_energy_best.pt` 在 CPU 上重新打分，输出 `outputs/energy_preference_validation_20260727/board_smoke_rollout_energy_scores.json`。
- 本次 Preference-order accuracy 结果已整理到 `outputs/energy_preference_validation_20260727/board_smoke_preference_order_accuracy.md/json`。严格 key contact 口径采用每条 episode 的左手 marker magnitude 最大窗口，`expert_margin` 排序正确 2/3，accuracy=66.67%；模型自选最高 quality 窗口同样为 2/3，accuracy=66.67%；全窗口正负两两比较为 1090/2436，accuracy=44.75%。所有窗口都被 scorer 判为 `pressure_too_small`，quality 绝对值约 0.024~0.032，说明该 smoke rollout 子集与训练分布不匹配，当前结果只能作为临时 validation，不应作为正式 rollout 结论。

2026-07-21
- 根据用户反馈“复制到飞书后公式形式没有出来”，新增 `paper/patent/第二实施例_飞书可复制版.md`。该版本去除 LaTeX 公式、代码块和复杂数学排版，将梯度、能量分数、速度修正等公式改写为普通文字表达，保留图9完整推理流程图和图10能量评分模型结构图的占位说明，便于直接粘贴到飞书文档。
- 整理专利第二实施例“接触质量能量模型 + 梯度引导式动作生成”方案说明，新增文档 `paper/patent/第二实施例_接触质量能量梯度引导方案说明.md`。内容包括：与第一实施例 DP+触觉前瞻+评分+reranking 的衔接关系、可直接放入专利正文的第二实施例描述、能量模型训练方式、技术效果、图9完整推理流程图占位说明、图10接触质量能量评分模型内部结构图占位说明。本次仅整理专利文字与图位需求，未修改模型代码。

2026-06-25
- 真机部署日志新增完整时间戳字段，用于分析 DP/PTG server-client 串行推理流程中的真实 episode 时间、推理步、执行步和控制周期耗时。
- 修改 for_show_xiaomi/server_rollout_logger.py：server 侧 force_trace.csv/npz/metadata.json 新增 wall_time_iso、episode_start/end_wall_time、episode_duration_s、server_obs_recv_wall_time、server_obs_recv_episode_t、server_obs_interval_ms、server_is_replan_step、server_chunk_index、server_chunk_action_index、server_replan_time_ms、server_step_time_ms、server_action_enqueue_wall_time；同时接收并保存 client obs 中携带的上一真实控制步 client_prev_* timing 字段。
- 修改 for_show_xiaomi/ws_client.py：client 侧 force_trace.csv/npz/metadata.json 新增 wall_time_iso、client_step_start/end_wall_time、client_obs_after_action_wall_time、client_step_episode_t、client_send_obs_time_ms、client_wait_action_time_ms、client_env_step_time_ms、client_step_pre_sleep_time_ms、client_control_sleep_ms、client_step_time_ms；并将上一控制步 timing 随下一帧 obs 发给 server。
- 修改 for_show_xiaomi/serve_board_dp_foresight_guided.py：board latent-energy guided/baseline serving 返回并保存 server timing，记录每步是否重新 DP 推理、action chunk 索引、chunk 内 action 索引、重推理耗时和 server 处理耗时。
- Review 后补充 for_show_xiaomi/ws_server.py：server socket 线程在真正收到 obs 时写入 _server_transport_recv_wall_time/_server_transport_recv_perf，guided server 优先用该时间作为 server_obs_recv_wall_time，并额外记录 server_loop_obs_start_wall_time，避免主推理循环延迟污染 obs 接收时间。
- 分析 20260625 nfe50 guided 三条真机擦黑板轨迹时发现 force_trace.csv 均为 300 行，metadata.json 显示 steps=300、stop_reason=max_timesteps；结合用户确认 client 端 max_steps=2000，确定截断来自 server 端 serve_board_dp_foresight_guided.py 的默认 --max_timesteps=300。
- 修改 for_show_xiaomi/guide_forshow.sh：所有擦黑板 latent-energy baseline/guided server 命令显式加入 --max_timesteps 2000，并在注释中说明 client 已经 2000 时只需改 server，避免后续真机长轨迹复测被 300 个 high-level action step 截断。
- 修改 for_show_xiaomi/serve_board_dp_foresight_guided.py：server_metadata 新增 max_timesteps 字段，之后每条 server-side metadata.json 可直接看到该 episode 的 server 上限，不再需要从 stop_reason 和代码默认值反推。
- 新增 for_show_xiaomi/merge_board_rollout_segments.py：把旧真机擦黑板 server-side segment 按时间连续性与 stop_reason 合并为真实 client trial；合并规则为同一 root/group 内按开始时间排序，前一段 stop_reason=max_timesteps 且相邻 gap<=2s 时继续合并，遇到 client_disconnected 结束真实 trial。
- 按用户要求只合并三组旧 guided 数据：oh4_dynamic32768_nfe30_h16_guided_g10_s001、oh4_dynamic32768_nfe50_h16_guided_g20_s001、oh4_dynamic32768_nfe100_h16_guided_g30_s001；输出到 /home/chenshuai/Project/output/board_force_rollouts_merged_trials/selected_nfe_guided，共 27 个 server segment 合并为 8 条 real_trial，并为每条生成 force_trace_merged.csv、force_trace_merged.png、summary.json 与总 summary markdown/json。
- 新增 for_show_xiaomi/analyze_board_success_force_smoothness.py：针对用户指定的三条成功擦黑板 real_trial 做 left Fz 平稳性分析，明确不使用坏掉的 right force 传感器；结合 left_fz、left_marker_mag_mean、eef_z 推断接触/擦拭段，并计算 left Fz std、相邻 |dFz|、短时残差波动、action step L2 平滑度。输出到 /home/chenshuai/Project/output/board_success_force_smoothness_leftfz，包含每条轨迹的 analysis_timeseries.csv、left_fz_action_smoothness.png、summary.json 和三轨迹对比图/总表。当前三条成功轨迹平稳性排序为 nfe50_trial002 最稳，其次 nfe30_trial002，nfe50_trial000 波动最大。
- 更新 for_show_xiaomi/analyze_board_success_force_smoothness.py 可视化：三条成功轨迹单图使用统一坐标轴，left Fz 固定为 0~25N、刻度 5N；短时波动 0~3、action delta 0~3、left marker 0~15、eef_z 0.12~0.24，便于横向直接比较。修复单图重画循环中 name 变量复用导致只覆盖最后一张图的问题，并重新生成全部 PNG。

2026-06-26
- 按用户要求基于 /media/chenshuai/EXTERNAL_USB/pih_dataset/260615_v8l_card/success 的 50 条刷卡 success HDF5 生成 100 条刷卡正样本 EEF 轨迹。先统计 success：平均长度约 428.7 steps、21.4s，起点在 reset 附近，接触/刷卡段 Z 约 166~171mm，主运动为 X 从约 379mm 刷到 187mm。
- 修改 data_collection/generate_trajectories.py：新增 card task 和 CardTrajectoryGenerator，读取 actions/eef_abs（缺失时回退 observations/proprio_eef），构建 success 平均模板；欧拉角先 unwrap 防止 rx 在 +/-pi 附近平均错误，保存前 wrap 回 [-pi, pi]；生成时从 success 残差采样并叠加低频平滑扰动，保留 reset 起点，并加入 48mm/s 保守速度上限，必要时自动重采样拉长轨迹。
- 生成输出到 outputs/card_pos_trajectory_20260626：100 个 card_pos_*.npy，前三条带 PNG 预览，并写入 README.md 与 summary.json。最终统计：长度 354~569 steps、平均 427.01 steps；最低 Z 165.627mm，最高 Z 229.459mm；最大速度 mean 40.054mm/s、p95 46.299mm/s、max 47.951mm/s，无样本超过 auto_replay dry-run 的 50mm/s 提示线。
- 使用 data_collection/auto_replay.py --dry_run 抽查 card_pos_000.npy 和修正前速度最高的 card_pos_015.npy，起点距 RESET 0.2mm、最低 Z 大于 166mm、最大速度分别 46.1/47.8mm/s，检查项均为通过。
- 根据用户反馈“轨迹要能做刷卡任务、步数和路线要和本来的差不多”，新增独立脚本 data_collection/generate_card_success_keepsteps.py。该脚本不再使用模板平均、不做时间拉长/重采样变长；直接读取每条 success 的 actions/eef_abs，保持原始步数和完整路线骨架，只加入极小低频扰动并锁住起末段。
- 生成新版刷卡正样本到 outputs/card_pos_keepsteps_20260626：100 条 card_pos_*.npy，50 条原始 success 每条使用两次；每条输出步数严格等于对应 source，步数范围 344..569。全量验证 length_ok=True，平均 XYZ 偏差 0.134mm，p95 均值 0.312mm，最大偏差 0.958mm。速度最大值 84.8mm/s 来自原始 success 本身，为保持原步数未做时间拉长。
- 撤销前一次错误集成：已从 origin/upstream 的 tacfore 分支追加 aa5b02e，删除误加入 git 的 data_collection/__pycache__/simple_record_pih_v8j(1).py、robotiq_gripper_server_no_ros .py 和错误工作记录条目；本地两个脚本恢复为 /home/chenshuai/Downloads 下原始副本后重新修改。
- 正确修改仅限两个用户指定脚本：data_collection/__pycache__/simple_record_pih_v8j(1).py 与 data_collection/__pycache__/robotiq_gripper_server_no_ros .py。数采脚本本地加载夹爪脚本，初始化 Robotiq 串口夹爪，Sigma7 get_action() 的 buttons 默认取第 0 个数值作为按钮幅度并映射到 0~235；夹爪 move 在后台线程节流发送，避免阻塞 20Hz 主控制循环。
- 数采开始从 Sigma7 按钮改为键盘 S：默认关闭 Sigma7 按钮遥操作门控和 END_ON_TELEOP_DISABLE，按 S 后才开始 warmup/写数据，q 仍结束 episode；保留 --teleop-enable-with-sigma7-button true 可恢复旧按钮门控。
- 数据格式：启用夹爪时 observations/proprio_joint 与 actions/joint_abs 从 7 维扩为 8 维，最后一维为夹爪位置；同时保存 observations/gripper_pos 与 actions/gripper_pos 方便检查。夹爪脚本 move() 限幅从 0~255 改为 0~235，符合用户给定完全打开/关闭范围。已用 AST 语法检查两个脚本通过。

2026-06-29
- 针对 260609/260610 擦黑板 DP ckpt 真机部署动作过慢的问题，按讨论采用 temporal_stride=3 重新训练方案；不删除原始 HDF5 数据，而是在训练 Dataset 内按 3 帧间隔取历史观测/触觉历史/未来动作，保证实验可复现且不破坏原始数据。
- 修改 diffusion/train_dp_tac_concat.py：新增 --temporal_stride 参数，默认 1 保持旧行为；stride=3 时 obs_horizon=4 对应 O(t-9),O(t-6),O(t-3),O(t)，tac_history=8 对应每 3 帧一帧的触觉历史，pred_horizon=16 对应 A(t),A(t+3),...,A(t+45)。
- 新增 scripts/train/train_dp_tac_concat_board_260609_260610_obs4_ph16_temporalstride3_dynamic32768_e1000.sh：沿用原数据集、Board VAE、rawimg200x266、obs_horizon=4、pred_horizon=16、dynamic_train_windows=32768、epochs=1000，但输出目录改为 temporalstride3，避免覆盖 dense baseline。
- 修改 for_show_xiaomi/serve_dp_policy.py 与 for_show_xiaomi/serve_board_dp_foresight_guided.py：部署端从 ckpt config.json 自动读取 temporal_stride，并按 stride=3 构建 DP obs_cond 与触觉历史缓存，避免训练/部署历史帧时间尺度不一致；metadata 同步记录 temporal_stride。
- 已验证：py_compile 通过；bash -n 新训练脚本通过；临时 HDF5 smoke test 验证 temporal_stride=3 时 qpos 历史索引为 [0,3,6,9]，action 标签索引为 [9,12,15,18]。
- 按用户要求给纯视觉官方对齐版 DP 增加跳步训练：修改 diffusion/train_dp_official.py，新增 --temporal_stride 参数，默认 1 保持原官方 dense 行为；stride=3 时 obs_horizon=2 对应 O(t-3),O(t)，pred_horizon=16 对应 A(t),A(t+3),...,A(t+45)，用于训练更快的纯视觉 board wiping baseline。
- 新增 scripts/train/train_dp_official_board_260609_260610_ph16_oh2_temporalstride3_e600_server.sh，服务器路径直接使用 /root/vtm 代码和 /mnt/robot-bionic-control-data/chenshuai18 下的 260609/260610 数据，输出到 tactileact_output/dp_official_vision_board_260609_260610_ph16_oh2_temporalstride3_e600。
- 修改 data_collection/__pycache__/simple_record_pih_v8j(1).py 的 Robotiq 夹爪脚本加载路径：优先兼容真机原始目录 realman_env/robot_servers/robotiq_gripper_server_no_ros .py，找不到时再回退到当前工作目录和脚本同目录，避免把手动夹爪脚本复制到采集脚本同目录或覆盖真机已有文件。
- 本地持续监控 temporal_stride=3 的 260609/260610 DP 训练，输出目录为 /media/chenshuai/SANDISK ELE/tactileact_output/dp_tac_concat_board_260609_260610_left_boardvae_rawimg200x266_ph16_oh4_temporalstride3_dynamic32768_e1000。验证集在 Ep70 达到当前 best val=0.005134；随后 Ep75 val=0.005694、Ep80 val=0.005805、Ep85 val=0.005816，连续 3 次 validation 未刷新且明显高于 best。结论：Ep70 后已进入 plateau/回升风险，继续跑满 1000 epoch 的边际收益很低，当前优先保留 Ep70 best ckpt 作为候选。
- 后续继续监控到 Ep90：train=0.004732 但 val=0.006190，已经连续 4 次 validation 未刷新且比 Ep85 进一步变差，说明训练 loss 下降没有转化为验证集改善。当前判断更偏向 Ep70 后过拟合/验证分布不再受益；如果不做新实验配置，继续跑当前 run 的收益很低。
- 按用户要求在验证集持续变差后提前停止本地 temporal_stride=3 训练。停止前最后完整验证点为 Ep130：train=0.003642、val=0.007890，已连续 12 次 validation 未刷新；当前 best 仍为 Ep70 val=0.005134。已停止 tmux session dp_stride3_260609 和 dp_val_plateau_monitor，GPU 显存从训练时约 23GB 释放到约 1.7GB。
- 针对 temporal_stride=3 DP 重新训练匹配的 foresight 和 latent-energy 引导 scorer。修改 TFAC_V5/dataset.py：ForesightEpisodicDataset 新增 temporal_stride，future tactile/qpos/action chunk 改为 t+offset+k*stride，支持 future_offset=0；保留 TactileVAE 每个 latent 的连续 8 帧编码窗口，不改变 latent 定义。修改 TFAC_V5/pretrain_latent_foresight_multistep.py 读取并打印 temporal_stride。
- 修改 board latent-energy scorer 数据链路：TFAC_V5/board_chunk_energy/dataset.py 的 ChunkDatasetConfig/ChunkIndexRow 新增 temporal_stride，窗口起点 stride 仍表示窗口起点间隔，temporal_stride 才表示窗口内部 t,t+3,...；TFAC_V5/board_latent_energy/dataset.py 与 vae_utils.py 按 row.temporal_stride 读取 action 和 marker latent；train.py/runtime.py 在 checkpoint 中保存并读取 temporal_stride，部署脚本 serve_board_dp_foresight_guided.py 启动时检查 DP/Foresight/scorer stride 是否一致。
- 新增 TFAC_V5/config_pretrain_foresight_board_multistep16_boardvae_marker_only_temporalstride3.json 与 scripts/train/train_foresight_board_multistep16_temporalstride3.sh。训练配置为 marker-only board VAE、chunk/predict horizon=16、future_offset=0、action_offset=0、temporal_stride=3，即 obs[t] + qpos/action[t,t+3,...,t+45] -> tactile[t,t+3,...,t+45]，部署时使用 alignment=none。
- 新增 scripts/train/train_board_latent_energy_temporalstride3.sh，训练 stride=3 latent-energy scorer。输出 /home/chenshuai/Project/output/board_latent_energy/ce_margin_temporalstride3_e40，best checkpoint 为 board_latent_energy_best.pt；manifest 中 row.temporal_stride=3，最终 val macro_f1=1.0、expert_vs_negative_auroc=1.0、expert_margin_auroc=1.0、expert_margin_gap_mean≈25.31。
- 完成 stride=3 foresight 训练，输出 /home/chenshuai/Project/output/foresight_ckpt/latent_foresight_board_260609_260610_multistep16_boardvae_marker_only_temporalstride3_e100_bs16_preload。训练 100 epoch，best 为 epoch 94，val total loss=0.1876，best checkpoint 为 foresight_best.ckpt。后期仍刷新 best，因此跑满 100 epoch，而不是在 55/72 附近提前停止。
- 联调 smoke：使用 DP /media/chenshuai/SANDISK ELE/tactileact_output/dp_tac_concat_board_260609_260610_left_boardvae_rawimg200x266_ph16_oh4_temporalstride3_dynamic32768_e1000/dp_best.pth，新 foresight 和新 scorer，命令中 --alignment none、num_inference_steps=8、guidance_steps=2，dry_run_dataset_smoke_pass=true，grad_finite_mean=1.0。结论：stride=3 DP + stride=3 foresight + stride=3 latent-energy scorer 的部署链路 shape/梯度/配置一致性通过。
- 2026-06-30：检查 /media/chenshuai/EXTERNAL_USB/pih_dataset/260625_v8j_caheiban/peg_in_hole_0625 数据，共 80 条 episode，全部可读，总帧数 55438；单条长度 min=503、median=680、mean≈693、max=914。pred_horizon=16 时 temporal_stride=1 的 required_span=16，总可用窗口 54238；temporal_stride=3 的 required_span=46，总可用窗口 51838；temporal_stride=6 的 required_span=91，总可用窗口 48238。按用户决定先不跳步，新增官方纯视觉 DP 的 val split 支持，并新增脚本 scripts/train/train_dp_official_pih_260625_v8j_caheiban_ph16_oh2_stride1_val5_e600.sh，使用 global+wrist、obs_horizon=2、pred_horizon=16、temporal_stride=1、val_ratio=0.05、e600 配置，输出到 /media/chenshuai/EXTERNAL_USB/pih_output/dp_official_vision_pih_260625_v8j_caheiban_ph16_oh2_stride1_val5_e600。

2026-06-30
- 按用户要求修改 /home/chenshuai/Downloads/simple_record_pih_v8l_forceviz(2).py 的跟随模式：将两处实际发送命令 self.arm.rm_movep_canfd(p.tolist(), 0) 改为 self.arm.rm_movep_canfd(p.tolist(), True)，注释同步从 follow=0 低跟随改为 follow=True 高跟随。该脚本位于 Downloads，不在当前 git 仓库中，因此仓库内仅记录本次操作。
- 按用户要求将 paper 目录下旧 CoRL/TacScore 论文稿件改写为 IEEE RA-L 初投稿 LaTeX 稿。调研确认 RA-L 初投稿/返修使用 RAS `ieeeconf` conference 模板，接收终稿再切 IEEE journal 样式；因此新增 `paper/ieeeconf.cls`、`paper/IEEEtran.bst`、`paper/IEEEabrv.bib`，并在 `paper/README_RAL.md` 记录模板来源与当前写作假设。
- 重写 `paper/main.tex`：从原先“TacScore 候选动作 reranking”主线改为当前“base policy + multi-step tactile foresight + TacQuality score guidance”的 Proactive Tactile Guidance (PTG) 主线；任务覆盖插插座、擦黑板、擦花瓶、刷卡和夹薯片/potato-chip grasping；真实结果未编造，表格和图保留为明确 placeholder，并区分离线 scorer/foresight 证据与待补的 paired real rollout 结论。
- 更新 `paper/references.bib` 顶部注释为 PTG RA-L draft；生成新的 RA-L 源码包 `paper/TacScore_RAL2026_source.zip`，保留原始 `paper/TacScore_CoRL2026.zip` 不覆盖。
- 本地监控 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260625_v8j_caheiban/peg_in_hole_0625` 的官方纯视觉 DP stride=1 训练。训练集 76 条 episode/51437 windows，验证集 4 条 episode/2801 windows；best val 在 Ep25 达到 0.006098。随后 val 长时间未刷新：Ep50 val=0.006228、Ep55 val=0.006230、Ep60 val=0.007081、Ep65 val=0.007077、Ep70 val=0.007954，而 train loss 仍下降到 Ep70 train=0.004132。按用户“变差就提前停止”的要求，已停止 tmux 训练，当前部署/评估优先使用输出目录中的 `dp_best.pth`，不建议使用 Ep70/topk train ckpt。
- 检查 `/media/chenshuai/czy_data22/pih_dataset/260630_v8j_huaping/peg_in_hole_0630` 数据：根目录 `episode_1.hdf5` 到 `episode_100.hdf5` 齐全且全部可读；显式排除子目录 `无夹取位置变化`，当前官方 DP 脚本只读取 dataset_dir 根目录下的 episode 文件，不递归读取该子目录。100 条数据总帧数 43742，长度 min=359、median=438.5、mean=437.42、max=518。按用户要求新增训练脚本 `scripts/train/train_dp_official_pih_260630_huaping_ph16_oh2_stride1_val5_e600.sh`：参数沿用上一次官方纯视觉 PIH 训练，global+wrist、pred_horizon=16、obs_horizon=2、temporal_stride=1、val_ratio=0.05、epochs=600，持续保存 `dp_best.pth`。已启动 tmux `dp_official_pih260630_huaping_stride1_val5_e600`，输出到 `/media/chenshuai/czy_data22/pih_output/dp_official_vision_pih_260630_huaping_ph16_oh2_stride1_val5_e600`；首轮 `Ep1 train=0.171951 val=0.040387`。根据用户后续要求，监控策略调整为至少训练到 Ep200；若 Ep200-Ep300 之间 val 基本不再刷新且趋势停滞/变差，则可在该区间停止，保留 best ckpt；若仍持续刷新 best，则继续训练。
- 为“下午数据” `/media/chenshuai/EXTERNAL_USB/pih_dataset/260625_v8j_caheiban/peg_in_hole_0625` 准备纯视觉 DP rerun 脚本 `scripts/train/train_dp_official_pih_260625_v8j_caheiban_ph16_oh2_stride1_val5_e600_rerun_min200.sh`，输出到不覆盖旧结果的 `..._rerun_min200` 目录，参数同样为 global+wrist、PH16/OH2、temporal_stride=1、val_ratio=0.05、epochs=600，监控策略至少跑到 Ep200。当前机器上 `/media/chenshuai/EXTERNAL_USB` 未挂载，`SANDISK ELE`、`czy_data21`、`czy_data2` 下也未找到 `peg_in_hole_0625`，因此该 rerun 等对应外盘/路径恢复后再启动。
- 新增并启动队列监控脚本 `scripts/train/monitor_pih_dp_queue_260630_then_260625.py`，tmux session 为 `pih_dp_queue_monitor_260630_then_260625`。监控策略：当前 260630_huaping 至少跑到 Ep200；Ep200 之后若 best val 已经 50 个 epoch 未刷新则发送 Ctrl-C 停止并保留 `dp_best.pth`；随后检测 260625 数据路径是否挂载，存在则自动启动 rerun，不存在则每 5 分钟等待。低磁盘空间时仅清理旧 `dp_epoch*.pth`，不删除 `dp_best.pth` 或 topk。

2026-06-30
- 按用户要求调研两个月内可做的触觉/视触觉 world model 研究方向，整理文档 `research/2026-06-30_两个月视触觉world_model调研与选题建议.md`。结论：不建议两个月内做大型 VT-WM/OmniVTA 式视频生成 world model；推荐做轻量级 action-conditioned tactile/force consequence model，以当前 vision/proprio/tactile + action chunk 预测 future tactile latent/marker/force/contact proxies，再接 TacQuality/force-quality energy 做 bounded DP guidance。主任务建议先定擦黑板，因为 force band、smoothness、contact gate 和现有真实 force trace 证据最完整；插孔可作为后续第二任务。

2026-07-01
- 按用户要求在 /home/chenshuai/Project/mi_pih/realman-infra 中不改原有采集文件，新增连续夹爪专用文件：realman_env/spacemouse/sigma7_expert_abs_gripper.py 与 simple_record_pih_v8j_gripper.py。
- 新 Sigma7 expert 从原 sigma7_expert_abs.py 复制而来，去掉 dhd.emulateButton(True) 对两指夹的二值化占用，新增 dhd.getGripperGap 连续读取，归一化为 get_gripper(): 0=闭合、1=张开；原 button0 仍保留给真实物理按钮/teleop 逻辑，避免捏两指夹导致 teleop 反复开关。
- 新采集脚本只导入 sigma7_expert_abs_gripper，并优先用 env.spacemouse.get_gripper() 控制 Robotiq；映射方向为 Sigma7 0=闭合/1=张开 -> Robotiq 235=闭合/0=打开，同时保留 buttons 控制回退。原 simple_record_pih_v8j(1).py、sigma7_expert_abs.py 未修改。
- 验证：PYTHONPYCACHEPREFIX=/tmp/mi_pih_pycache python -m py_compile 两个新脚本通过。mi_pih 仓库已有用户未提交改动和未跟踪文件，本次只提交两个新增文件，commit 为 e90fd9f。

2026-07-02
- 修复 `data_collection/__pycache__/simple_record_pih_v8j(1).py` 中力/夹爪可视化没有显示的问题：原脚本 `--visualize-ft` 依赖缺失的 `visualize_ft.py`，导致只有两个相机窗口，没有六维力曲线窗口。
- 在 v8j 数采脚本内置 `ForceGripperVisualizer`，使用 OpenCV 单独显示 `Fx/Fy/Fz/Tx/Ty/Tz` 实时曲线，并在同一窗口显示 Robotiq 夹爪命令位置条（0=open，235=close）。每个控制 step 会 push 当前 `obs['ft']` 与 `current_gripper_pos`。
- 调整命令行行为：`--visualize` 现在默认同时启动相机/触觉窗口和六维力/夹爪窗口；`--visualize-ft` 仍可单独打开力/夹爪窗口；新增 `--no-visualize-ft` 用于只看相机窗口。已用 `python -m py_compile` 与 `git diff --check` 验证通过。

2026-07-03
- 排查近期 PIH 官方纯视觉 DP 训练中 validation loss 早期最优、后期回升的问题。结论：更像是少量 episode、小验证集、强重叠滑窗和 306M 级别视觉 DP 容量造成的过拟合/验证噪声，不是当前发现的采样窗口代码错误。已整理到 `research/2026-07-03_PIH_DP过拟合与刷卡训练记录.md`。
- 针对刷卡数据先采用保守 success-only 纯视觉 DP baseline：新增 `scripts/train/train_dp_official_card_260629_success_ph16_oh2_stride1_val15_e600.sh`，只读取 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/peg_in_hole_0629/success`，不混入 `bounce_hengxiang`、`bounce_jiaozhun` 或 260701 的 `bounce_*` 数据；参数为 global+wrist、PH16/OH2、n_action_steps=8、temporal_stride=1、val_ratio=0.15、epochs=600，输出到 `/media/chenshuai/EXTERNAL_USB/pih_output/dp_official_vision_card_260629_success_ph16_oh2_stride1_val15_e600`。
- 明确记录：纯视觉 DP 不需要 TactileVAE；如果后续做 tactile+vision DP、foresight 或引导，刷卡任务应训练 card-specific TactileVAE 后再训练对应 policy/foresight/scorer。

2026-07-05
- 按用户要求准备新任务 task-local TactileVAE 训练。新增 `scripts/pretrain/run_tactile_vae_huaping_260630_left.sh`：只使用 `/media/chenshuai/czy_data22/pih_dataset/260630_v8j_huaping/peg_in_hole_0630` 根目录 100 条 episode，显式排除 `无夹取位置变化`，输出 `/media/chenshuai/czy_data22/pih_output/tactile_vae_huaping_260630_left_tw8_ld16_s2_e150`。
- 新增 `scripts/pretrain/run_tactile_vae_card_260615_260626_260629_260701_left.sh`：使用 260701 card bounce、260629 card bounce/success、260626 replay card、260615 card bounce/success 训练 card-specific marker TactileVAE，显式排除 `260629_v8j_card/260630_v8j_huaping` 这个嵌套的非刷卡目录，输出 `/media/chenshuai/EXTERNAL_USB/pih_output/tactile_vae_card_260615_260626_260629_260701_left_tw8_ld16_s2_e150`。
- 检查夹薯片 260702/260703 HDF5 后发现关键阻塞：数据只有 `observations/tac/left/img` 和 `observations/tac/right/img`，没有现有 TactileVAE/DP concat 链路需要的 `observations/tac/{left,right}/marker_offset`。因此当前不能直接训练“和之前一样”的 marker TactileVAE + frozen concat DP；需要用户确认是补算/重采 marker_offset，还是改走 raw tactile image DP/新 tactile image VAE。详细记录在 `research/2026-07-05_任务TactileVAE训练计划与夹薯片数据阻塞.md`。
- Huaping marker TactileVAE 已完成训练，输出 `/media/chenshuai/czy_data22/pih_output/tactile_vae_huaping_260630_left_tw8_ld16_s2_e150/best_tactile_vae.pt`，best val loss=0.025630，final cosine=0.979，final angular error=6.5°。
- 按用户“跳步后任务步数约 200-300”的要求，为 huaping/card 有触觉 DP 选择 `temporal_stride=2`。新增 huaping DP 脚本 `scripts/train/train_dp_tac_concat_huaping_260630_left_huapingvae_ph16_oh2_stride2_dynamic32768_e600.sh`；新增 card positive-only DP 脚本 `scripts/train/train_dp_tac_concat_card_positive_left_cardvae_ph16_oh2_stride2_dynamic32768_e600.sh`；新增队列脚本 `scripts/train/run_huaping_card_tactile_dp_queue.sh`，等待 card VAE 完成后顺序训练 huaping DP 与 card DP，避免单 GPU 同时跑两个大 DP。
- 训练状态复查：当前没有 tmux 训练进程，GPU 空闲；card TactileVAE 在 Ep20 后写 `/media/chenshuai/EXTERNAL_USB/pih_output/tactile_vae_card_260615_260626_260629_260701_left_tw8_ld16_s2_e150/train_history.json` 时报 `FileNotFoundError` 中断，结合 `mount/findmnt` 显示 `EXTERNAL_USB`、`czy_data22`、`SANDISK ELE` 都是 `ro` 只读挂载，判断为外接盘掉盘/只读挂载导致输出目录不可写。按用户最新要求更新 DP 数据源：card tactile DP 只用 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260626_replay_card/card_pos_keepsteps_20260626` 与 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/peg_in_hole_0629/success`；huaping tactile DP 改用 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/260630_v8j_huaping/peg_in_hole_0630` 根目录 100 条 episode，排除子目录 `无变化`。VAE 仍按任务使用全量可用 marker tactile 数据；DP 继续采用 `temporal_stride=2`，目标为下采样后约 200-300 步。
- 按用户“外接盘只读数据，ckpt/cache 写内置盘”的要求，将 huaping/card TactileVAE、huaping/card tactile DP、DP 队列脚本的默认 `OUTPUT_ROOT` 统一改为 `/home/chenshuai/Project/output/pih_tactile`。新增完整单 GPU 队列脚本 `scripts/train/run_huaping_card_tactile_full_queue_internal.sh`：顺序执行 huaping TactileVAE、card TactileVAE、huaping tactile DP、card tactile DP；外接盘仅作为只读数据源，所有日志、VAE ckpt、DP ckpt 和 image cache 均写入内置 NVMe。数据复查：huaping 指定路径 100 条 episode，card replay 53 条，card success 20 条，左手 `marker_offset` 均存在且无坏文件；`temporal_stride=2` 后平均步数约 huaping 219、card replay 216、card success 230，符合用户希望的 200-300 步范围。
- 已启动 tmux `pih_tactile_full_internal`，队列日志 `/home/chenshuai/Project/output/pih_tactile/huaping_card_tactile_full_queue_20260705_234614.log`。Huaping TactileVAE 在内置盘完成：输出 `/home/chenshuai/Project/output/pih_tactile/tactile_vae_huaping_260630_left_tw8_ld16_s2_e150`，best val loss=0.025162，final cosine=0.979，final angular error=6.5°。Card TactileVAE 在内置盘完成：输出 `/home/chenshuai/Project/output/pih_tactile/tactile_vae_card_260615_260626_260629_260701_left_tw8_ld16_s2_e150`，best val loss=0.011714，final cosine=0.994，final angular error=3.7°。随后队列已进入 huaping tactile DP，使用 `temporal_stride=2`、`dynamic_train_windows=32768`、`max_val_windows=4096`，正在将 huaping 图像 cache 写入 `/home/chenshuai/Project/output/pih_tactile/cache/dp_tac_concat_huaping_260630_external_rawimg200x266_fp16`。

2026-07-06
- Huaping tactile DP 在内置盘 image cache 完成后进入训练，输出 `/home/chenshuai/Project/output/pih_tactile/dp_tac_concat_huaping_260630_external_left_huapingvae_rawimg200x266_ph16_oh2_stride2_dynamic32768_e600`。验证曲线：Ep1 val=0.053426，Ep5=0.019377，Ep10=0.018018，Ep15=0.016401，Ep20=0.016218（当前 best），之后 Ep25=0.016392、Ep30=0.016716、Ep35=0.017476、Ep40=0.018766、Ep45=0.020678、Ep50=0.021849，train loss 仍从 Ep20 的 0.012318 下降到 Ep50 的 0.008010，判定已明显过拟合。已在 Ep51 中途停止 huaping DP，保留 `/home/chenshuai/Project/output/pih_tactile/dp_tac_concat_huaping_260630_external_left_huapingvae_rawimg200x266_ph16_oh2_stride2_dynamic32768_e600/dp_best.pth` 作为部署/评估 ckpt。
- Card tactile DP 首次按 lazy_images 启动后速度过慢：约 5 分钟仅到 Ep1 batch100，估算 1 epoch 约 20 分钟。由于当前 card policy 数据只含 `260626_replay_card/card_pos_keepsteps_20260626` 与 `260629_v8j_card/peg_in_hole_0629/success` 两个唯一 basename 目录，不再存在多个 `success` cache 命名冲突，因此将 card DP 脚本改为使用内置盘 image cache，输出目录改为 `/home/chenshuai/Project/output/pih_tactile/dp_tac_concat_card_positive_left_cardvae_rawimg200x266_ph16_oh2_stride2_cache_dynamic32768_e600`，cache 路径 `/home/chenshuai/Project/output/pih_tactile/cache/dp_tac_concat_card_positive_rawimg200x266_fp16`。已启动 tmux `pih_card_tactile_cache_internal`，当前处于 card image cache 构建阶段。
- Card tactile DP cache 版本已完成 image cache：构建 146 个缓存文件，约 20G，训练集 66 eps/26816 windows，验证集 7 eps/3077 windows。初期验证正常下降：Ep1 train=0.239161 val=0.054702，Ep5 train=0.016576 val=0.019457，Ep10 train=0.012562 val=0.016567（当前 best），暂未出现 huaping tactile DP 那种验证回升过拟合信号。
- 用户指出 card tactile DP 不能过早因 Ep20/Ep25 附近的验证波动停止，至少先训练到 Ep100。监控策略调整为：Ep100 前不早停，只记录 Ep25/Ep50/Ep75/Ep100 的 train/val/best 趋势；Ep100 后再结合验证曲线、best 刷新间隔和训练 loss 判断是否继续。
- Card tactile DP 后续验证证实 Ep20 的轻微回升只是早期验证噪声：Ep15 train=0.011083 val=0.015154，Ep20 train=0.009440 val=0.015604 未刷新，但 Ep25 train=0.008469 val=0.013615 再次刷新 best。结论：不能依据单个早期验证点判断过拟合，继续至少训练到 Ep100。
- Card tactile DP Ep30 记录：train=0.007948，val=0.016781，未刷新 Ep25 best=0.013615。该点相对 Ep25 明显回升，但根据用户要求 Ep100 前不做早停，只作为早期验证波动/潜在过拟合信号记录，继续观察 Ep35/Ep40/Ep50/Ep75/Ep100。
- Card tactile DP Ep35/Ep40 记录：Ep35 train=0.007580 val=0.015869，Ep40 train=0.006544 val=0.016195，连续 3 个验证点未刷新 Ep25 best=0.013615。当前已出现 train 下降而 val 未同步改善的过拟合风险，但仍按用户要求继续至少到 Ep100，`dp_best.pth` 保持为验证最优 ckpt。
- Card tactile DP 已按“至少训练到 Ep100，之后若明显变差则停止”的策略在 Ep137 中途停止。关键验证点：Ep100 train=0.003608 val=0.032585，Ep105 val=0.033330，Ep110 val=0.030388，Ep115 val=0.038633，Ep120 val=0.033464，Ep125 val=0.037579，Ep130 val=0.037895，Ep135 train=0.002867 val=0.038099；best 始终停留在 Ep25 val=0.013615。结论：Ep25 后没有再泛化改善，Ep100 后验证 loss 已显著恶化，当前应使用输出目录中的 `dp_best.pth`，不建议使用 `dp_latest.pth`。
- 按用户要求先补训擦花瓶和刷卡的 matched foresight，评分模型暂不训练，后续部署/评估时临时复用已有评分。新增 huaping/card stride=2 foresight 配置与队列脚本：huaping 使用 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/260630_v8j_huaping/peg_in_hole_0630` 根目录 100 条 episode（因根目录有直接 episode 文件，不递归读取 `无变化`）；card 使用 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260626_replay_card/card_pos_keepsteps_20260626` 与 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260629_v8j_card/peg_in_hole_0629/success` 两组 positive policy 数据。两者均使用 task-local TactileVAE、marker-only、`future_offset=0`、`action_offset=0`、`temporal_stride=2`、horizon=16，目标对齐当前 stride-2 tactile DP。
2026-07-29
- 按用户要求检查本机 Codex 模型配置。当前全局配置位于 `/home/chenshuai/.codex/config.toml`，原模型为 `gpt-5.5`，本次已改为 `gpt-5.6`；同时将 `[tui.model_availability_nux]` 中的模型提示项从 `gpt-5.5` 更新为 `gpt-5.6`。修改前备份保存为 `/home/chenshuai/.codex/config.toml.bak-20260729-gpt55`。

- Huaping matched foresight 已完成训练，输出 `/home/chenshuai/Project/output/foresight_ckpt/latent_foresight_huaping_260630_huapingvae_marker_only_temporalstride2_e100_bs16_preload`，保存 `foresight_best.ckpt` 与 `foresight_last.ckpt`；best clean val=0.7619，best epoch=88，最后 Ep99 train=0.4335、val=0.8565。后半段有验证回升，部署/评估应优先使用 `foresight_best.ckpt`。
- Card matched foresight 已完成训练，输出 `/home/chenshuai/Project/output/foresight_ckpt/latent_foresight_card_positive_cardvae_marker_only_temporalstride2_e100_bs16_preload`，保存 `foresight_best.ckpt` 与 `foresight_last.ckpt`；训练数据为 73 条 positive episode（260626 replay 53 条 + 260629 success 20 条），train/val=65/8。best clean val=0.3911，best epoch=68；最后 Ep99 train=0.2835、val=0.5014。结论：card foresight 后期 train 持续下降但 val 回升，存在过拟合，部署/评估同样优先使用 `foresight_best.ckpt`。本轮未训练评分模型，后续暂时复用已有评分。
- 分析 v8j 擦花瓶与刷卡 HDF5 数据，输出到 `outputs/v8j_huaping_card_analysis/`。覆盖 huaping main 100 条、huaping 无夹取位置变化 30 条、card success/bounce_hengxiang/bounce_jiaozhun 各 20 条。关键结论：两类任务 `actions/eef_abs` 与 `observations/proprio_eef` 对齐，刷卡 `actions/joint_abs` 与实际关节对齐；但花瓶 `actions/joint_abs` 存在 IK/关节解跳变，action joint step max median 约 213-227 deg，而真实 measured joint step max median 约 1.1-1.2 deg。因此花瓶不建议直接用 `actions/joint_abs` 训练 joint_abs DP，建议使用 eef action 或基于 `observations/proprio_joint` 重新生成 joint label。

2026-07-07
- 检查新采集数据 `/media/chenshuai/SANDISK ELE/数据采集/260707_v8j_huaping/peg_in_hole_0707`，共 8 条 episode。该批 HDF5 没有 `debug/` 组，因此不能直接查看 `rm_movej_canfd` 返回码或实际下发 joint target；但从已保存的 `actions/joint_abs` 与 `observations/proprio_joint` 对比看，旧版花瓶数据中 200+ deg 的 IK 分支跳变没有再出现。全局最大相邻 `joint_abs` 跳变为 ep4 step281->282 的 10.81 deg，对应真实 qpos 同步变化 4.45 deg；全体 episode 中 `joint_abs` 相邻跳变 >30 deg 与 >150 deg 的次数均为 0。图和摘要输出到 `outputs/v8j_260707_huaping_analysis/`。
- 检查 debug 版单条数据 `/home/chenshuai/Downloads/episode_0(2).hdf5`。该文件包含 `debug/cmd_joint_abs`、`debug/cmd_ret` 等字段，长度 337 步；`cmd_mode` 全部为 1（movej_canfd），`cmd_ret` 全部为 0。`actions/joint_abs` 与 `debug/cmd_joint_abs` 完全一致（max diff=0），最大相邻 joint action/cmd 跳变 4.69 deg，最大真实 qpos 跳变 4.82 deg，>30 deg 与 >150 deg 跳变均为 0。最大 command-qpos 滞后约 9.38 deg（step316附近），属于跟随滞后而非 IK 分支跳。图和摘要输出到 `outputs/debug_episode_0_2_analysis/`。
- 继续检查 debug 版单条数据 `/home/chenshuai/Downloads/episode_3(1).hdf5`。该文件长度 543 步；`cmd_mode` 全部为 1（movej_canfd），`cmd_ret` 全部为 0，子步返回码除占位 `-999` 外无异常。`actions/joint_abs` 与 `debug/cmd_joint_abs` 完全一致（max diff=0），最大相邻 joint action/cmd 跳变 3.17 deg，最大真实 qpos 跳变 3.09 deg，>30 deg 与 >150 deg 跳变均为 0。最大 command-qpos 滞后约 6.59 deg（step502附近），判断为正常跟随滞后而非 IK 分支跳。图和摘要输出到 `outputs/debug_episode_3_1_analysis/`。
- 按用户要求把旧花瓶数据中 `actions/joint_abs` 本身跳变最大的 episode 画成和 debug 图相同风格的 full/zoom 图。选出的 Top5 为 huaping_main ep46 step365（249.52 deg）、ep54 step394（246.91 deg）、ep90 step386（246.90 deg）、ep39 step402（246.76 deg）、huaping_no_grasp_change/当前盘中 `无变化` ep2 step359（245.68 deg）。这些旧文件没有 `debug/` 组，因此图中只能对比 `actions/joint_abs` 与 `observations/proprio_joint`；共同特征是 action/cmd label 有约 245-250 deg 分支跳，而真实 qpos 同步跳变仅 0.16-0.89 deg。图和汇总输出到 `outputs/v8j_large_joint_jump_like_debug_plots/`。
- 查看 `/home/chenshuai/Downloads/simple_record_pih_v8l_forceviz(2)(1).py` 后确认：该 v8l/forceviz 采集脚本实际控制路径使用 `rm_movep_canfd` 下发末端笛卡尔位姿（pos+quat），没有下发 `actions/joint_abs`；而保存 `actions/joint_abs` 时是在记录阶段用 `rm_algo_inverse_kinematics` 对 `cmd_pose_t` 额外反解得到。因此如果旧数据由该脚本采集，真实机械臂运动正常与 HDF5 中 `actions/joint_abs` 出现 IK 分支跳并不矛盾；这些跳变应视为 joint_abs label/普通 IK 分支选择问题，不是当时实际发送的 joint command。
- 检查刷卡数据 `/media/chenshuai/SANDISK ELE/数据采集/260629_v8j_card` 与 `/media/chenshuai/SANDISK ELE/数据采集/260701_v8j_card` 的 `actions/joint_abs` 连续性。260629 三组共 60 条（success/bounce_hengxiang/bounce_jiaozhun）均未出现 >30 deg 跳变，最大 action jump 分别约 7.33/3.75/5.44 deg，基本正常。260701 两组共 60 条存在同类 IK label 分支跳：bounce_hengxiang 30 条中 4 条 >150 deg，最大 ep28 step363 为 244.72 deg、对应 qpos 仅 0.35 deg；bounce_jiaozhun 30 条中 5 条 >150 deg，另有 1 条约 45 deg，最大 ep46 step453 为 242.64 deg、对应 qpos 仅 0.14 deg。输出汇总、异常详情和 Top5 full/zoom/delta 图到 `outputs/v8j_card_260629_260701_joint_check/`。
- 为项目网页视频素材做 v8j 画屏/擦黑板可视化试验。新增 `scripts/make_camera_marker_video.py`：从单条 HDF5 中读取 `observations/images/global` 和 `observations/tac/{side}/marker_offset`，导出左侧主相机、右侧 9x9 marker 相对初始帧位移箭头/热图的并排 mp4。初步检查 `/media/chenshuai/SANDISK ELE/数据采集/260707_v8j_huaping/peg_in_hole_0707` 共 8 条 episode，主相机与左右 marker 字段齐全；右手 marker 平均幅值高于左手，因此默认使用 `observations/tac/right/marker_offset` 作为网页演示触觉视图。
- 根据用户反馈，将 marker 可视化脚本扩展为 `--style deformed-grid`：右侧不再只画密集箭头，而是画淡灰色初始 9x9 网格、当前 marker 点、变形网格、以及从初始点到当前点的短位移箭头；该风格更适合作为项目网页视频素材。保留原 `heatmap-arrows` 模式，并继续支持 `--raw-marker` 用于直接展示 HDF5 保存的原始 `marker_offset`。
- 按用户要求准备 260707 刷卡触觉版训练：数据使用 `/media/chenshuai/czy_data22/pih_dataset/260707_v8j_card/peg_in_hole_0707/success` 60 条与 `ercicharu` 30 条，`pengzhuang` 当前无直接 episode 文件；`/media/chenshuai/czy_data22` 当前为只读挂载，因此 ckpt/cache/log 默认写入 `/home/chenshuai/Project/output/pih_tactile`。新增 `scripts/pretrain/run_tactile_vae_card_260707_left_tw2.sh`，训练 card-specific left marker TactileVAE，`temporal_window=2`、`sample_stride=1`、默认 150 epoch；新增 `scripts/train/train_dp_tac_concat_card_260707_left_cardvae_ph16_oh2_stride1_tachist2_dynamic32768_e600.sh`，训练 tactile+vision DP，`temporal_stride=1`、`tac_history=2`、`pred_horizon=16`、`obs_horizon=2`、600 epoch；新增 `scripts/train/run_card_260707_tactile_full_queue.sh`，单 GPU 顺序执行 VAE 后再执行 DP。
- 260707 card TactileVAE 已完成：输出 `/home/chenshuai/Project/output/pih_tactile/tactile_vae_card_260707_left_tw2_ld16_s1_e150`，best val loss=0.003317，final cosine=0.998，final angular error=1.9°。DP 初次以 batch=64 启动时在 Ep1 backward OOM；原因是当前 GPU 已有 8773/8774/8776 等服务和一个 6GB 进程占显存，batch=64 的 DP 本进程约需 10GB。将 260707 card DP 脚本的默认 `BATCH_SIZE` 改为 32，并保留环境变量覆盖能力；image cache 已完整构建 90 episode × 2 cameras = 180 个文件，可直接复用。
- 由于 batch=32 在当前显存占用下仍差约 20MB OOM，将 260707 card DP 脚本增加 `NO_EMA` 开关并默认关闭 EMA，以减少训练期影子模型显存；`dp_best.pth` 仍按 validation loss 保存 raw policy。若后续清掉 8773/8774/8776 等服务释放显存，可用 `NO_EMA=false BATCH_SIZE=64` 重新跑同脚本。

2026-07-08
- 按用户要求准备多任务“假推理/回放 + 真机执行数据记录”。新增 `for_show_xiaomi/serve_hdf5_replay_record.py`：支持从 HDF5 回放 `actions/joint_abs` 或由 `actions/eef_abs` 转 `eef_rel`，每次 client 连接执行一条 episode，并为每条真实执行保存独立目录，包含 `metadata.json`、`trace.csv`、`trace.npz`；`trace.npz` 保存 qpos/eef/action/末端 ft/左右触觉 force6d/左右 marker_offset 完整序列。新增 `for_show_xiaomi/ws_client_record.py`：真机端记录版 client，强制 `Config.SAVE_FT=True`，并向 server 发送 `ft`、`tac/*/force6d`、`tac/*/marker_offset`。新增 `scripts/run_multitask_replay_record_servers.sh`：一键启动 caheiban 8781(joint)、huaping 8782(eef_rel，避开旧数据 joint_abs IK 跳变)、card260707 8783(joint) 三个回放记录 server。
- 完善多任务回放记录链路：`serve_hdf5_replay_record.py` 改为按 data_dir 输入顺序收集 episode，保证 260707 刷卡先回放 success 再回放 ercicharu；marker_offset 每一步都写入，缺失时用 NaN 占位，避免 trace 时间轴错位；额外保存 `joint_current`。`ws_client_record.py` 默认强制 `HIGH_FOLLOW=True`（匹配 v8j 真机回放），并显式请求触觉 `img/depth/marker_offset/force6d` 四种模态。已通过 py_compile、bash -n、git diff --check 和本地 RolloutRecorder smoke；正式 tmux server 已启动：8781 caheiban_260625 joint 80 episodes，8782 huaping_260630 eef_rel 100 episodes，8783 card_260707 joint 90 episodes（当前首条为 success/episode_0.hdf5）。

2026-07-09
- 按用户要求将 `outputs/foresight_retrain_20260708/trial_marker_videos` 中已有 t+1 GT/foresight marker 预测结果重新渲染为展示度更高的 deformed-grid/流形网格风格。新增 `scripts/render_trial_marker_deformed_grid_videos.py`，直接读取 `marker_prediction_tplus01.npz`，不重新推理；每条完整 trial 输出 `gt_marker_deformed_grid_tplus01.mp4`、`foresight_pred_marker_deformed_grid_tplus01.mp4`、`gt_vs_pred_marker_deformed_grid_tplus01.mp4` 三个视频。输出目录为 `outputs/foresight_retrain_20260708/trial_marker_videos_deformed_grid`。本次共生成 15 条 trial、45 个 mp4，校验全部视频可打开且帧数与对应 npz 的 marker 帧数一致。
- 针对 `caheiban_260609/20260708_180826_port8781_episode_6` 手机实拍与 GT/foresight marker 合成对齐问题，修正 `scripts/compose_phone_marker_video.py`：新增 `--trace-csv` 和 `--marker-sampling hold`，使用 replay `trace.csv` 的真实 `wall_time` 轴采样 marker，而不是均匀 step 线性拉伸；因此每 12 step 后约 0.3s 的执行停顿会在 marker 视频中 hold，首尾与手机视频锁齐。重新输出 3x/4x trace-aligned 版本到 `outputs/foresight_retrain_20260708/phone_marker_composite/caheiban_260609/`。
- 新增 `scripts/render_trial_force_curve_video.py`，读取同一 trial 的 `trace.npz/trace.csv`，将末端六维力中的 `ft[:, :3]` 渲染成动态 XYZ 力曲线视频。输出 `force_xyz_trace_aligned_phone3x.mp4` 与 `force_xyz_trace_aligned_phone4x.mp4`，帧率 24fps，时长分别与 3x/4x 手机-marker 合成视频一致。数值检查：该 trial 865 step，trace 动作段 125.63s，检测到 74 个大于 0.3s 的长间隔；左手 marker p95 接触开始约 step 144，峰值 step 705，末帧回落到约 0.39。
- 按用户要求比较末端力与夹爪触觉力平稳性，并将力曲线改为支持只看 Z 轴。扩展 `scripts/render_trial_force_curve_video.py`：新增 `--component xyz|z` 与 `--smooth-window`，可画 raw 淡线 + 平滑主线。对 `caheiban_260609/20260708_180826_port8781_episode_6` 生成 `force_z_smooth15_trace_aligned_phone3x.mp4` 与 `force_z_smooth15_trace_aligned_phone4x.mp4`；15-step 移动平均将末端 Fz 相邻 step 抖动 RMS 从约 1.44 降到约 0.26。导出统计表 `force_stats_episode6.csv/json`，结论：按相邻 step 抖动看左夹爪 Fz 最平稳，末端 Fz 抖动最大。
- 按用户要求将黑板演示素材接入小米内网 `vtm` 项目页仓库的 `ProjectPage` 分支。本地仓库 `/home/chenshuai/Project/TactileACT-cs/tmp_vtm_ProjectPage` 中新增 `static/videos/board_real.mp4`（真实执行，4x speed 标记）与 `static/videos/board_viz.mp4`（GT/Pred marker、末端力、tactile score、adaptive guidance scale 实时可视化），并将 `index.html` 的 Board Wiping 两个 placeholder 替换为实际 `<video>`。已做页面长截图检查，ProjectPage 仓库本地提交 `ca57b5f Add board wiping demo videos`；按用户要求未 push。

2026-07-14
- 按用户要求补黑板 stride3 guidance 离线 debug。新增 `scripts/eval_board_stride3_val_guidance_debug.py`：读取 stride3 latent energy scorer 的 `manifest.json` 验证集 split，自动将缺失的 `/home/chenshuai/data/dataset` 路径映射到 `/media/chenshuai/EXTERNAL_USB/pih_dataset`；对 val rows 计算 Foresight+scorer 的动作梯度热力图，并结合已有真实 stride3 DP denoising 日志画四联图（score before/after、`||grad score||` 与 `lambda||grad score||`、action-dim guidance heatmap、DP direction vs guidance direction）。正式小样本输出目录为 `outputs/board_stride3_val_guidance_debug_20260714/`，16/16 个验证窗口成功计算梯度。scorer final val acc=1.0、macro-F1=1.0、AUROC=1.0；val gradient norm mean=3.426，`lambda=0.003` 后 mean=0.01028；dense guided step score delta mean=0.01981；alignment mean by guided step 约 `[-0.000013, -0.06123, -0.01245]`。heatmap 显示主要作用在 j2/j4 维度。当前机器无 CUDA，且原 stride3 DP ckpt `/media/chenshuai/SANDISK ELE/.../dp_best.pth` 未挂载，因此 A/B/D 复用已有真实 DP 日志，C 为当前 val rows 重新计算；结论文档记录在 `research/2026-07-14_board_stride3_val_guidance_debug.md`。
- 按用户要求扩大黑板 stride3 验证集 action-gradient 样本量，运行 `scripts/eval_board_stride3_val_guidance_debug.py --val_rows_per_class 32`，输出 `outputs/board_stride3_val_guidance_debug_20260714_n128/`。本次每类 32 个 validation windows，总计 128/128 成功计算。mean `||grad score||` 从 16-window 版本的 3.426 稳定到 2.4475，mean `lambda||grad score||` 为 0.00734，max `lambda||grad score||` 为 0.04089；主导动作维度仍集中在 j2/j4，其中 j4=77/128、j2=51/128。按类别看 expert/pressure_too_large 更偏 j2，pressure_too_small/pressure_unstable 更偏 j4。结论：大样本 heatmap 仍支持 guidance 是局部动作维度修正，不是全维度乱拉；A/B/D 仍需等原 stride3 DP ckpt 挂载后才能对更多 val windows 重新采样。

2026-07-15
- 按用户要求将黑板 stride3 guidance 诊断结果接入项目页，最终采用静态论文图而不是视频。将 `outputs/board_stride3_val_guidance_debug_20260714_n128/board_stride3_val_guidance_debug_overview.png` 复制到 `tmp_vtm_ProjectPage/static/images/board_stride3_guidance_diagnostics_overview.png`，并在 `tmp_vtm_ProjectPage/index.html` 的 Results 区域新增 `Offline Guidance Diagnostics` 小节：只保留一张 A/B/C/D 四联图，不再单独重复展示 heatmap。网页说明明确写清数据来源：score/norm 来自 stride3 DDPM denoising logs，C panel heatmap 来自 128 个 validation windows，alignment 是 3-window rollout audit；单位说明为 score delta 无量纲、norm 位于归一化 action/score 空间、heatmap 为 `lambda * grad score`、alignment 为 cosine similarity。已用 Chrome headless 长截图检查布局，项目页已推送到远端 `ProjectPage`。
- 复查项目页 `Offline Guidance Diagnostics` 区块后，将 section lead 中较强的 `verify` 改为更保守的 `evaluate`，并在 Figure 5 下新增一条 `Takeaway` 结论：该离线分析支持 guidance 主要通过小幅、受限、动作局部的修正提升 learned contact-quality score，而不是全局重塑或重排序完整轨迹。该表述保留 offline evidence 边界，不宣称真实机器人成功率提升。
- 根据用户反馈继续精简项目页 guidance 诊断区块：移除所有可见的 `stride-3/stride3` 表述（该 stride 只是加速采样/推理的实现设置，不作为方法卖点展示），删掉 `alignment is a 3-window rollout audit` 和 `Units` 说明卡片，改为在 lead 中简洁说明该诊断使用 offline denoising logs 与 128 validation windows。重新导出四联图，将图内标题从 `Blackboard Stride-3 Offline Guidance Diagnostics` 改为 `Blackboard Offline Guidance Diagnostics`，并把网页图片路径改为 `static/images/board_guidance_diagnostics_overview.png`。
- 继续按用户反馈去掉项目页 guidance 诊断区块中的 `Offline/offline` 字眼，将标题改为 `Guidance Diagnostics`，lead 改为 `The diagnostic uses denoising logs and 128 validation windows`，caption 与 takeaway 中也移除 `Offline/offline`。同时将图内标题改为 `Blackboard Guidance Diagnostics`。项目页已推送到远端 `ProjectPage`，最新提交 `41ad6e9`。

2026-07-16
- 按用户要求根据项目主页 `tmp_vtm_ProjectPage/index.html` 和旧论文包 `paper/TacScore_RAL2026_source.zip` 重写 RA-L 论文稿。方法名/标题暂按网页统一为 `ForeTac: Steering Robot Actions with Predicted Contact Consequences`；投稿格式沿用 RA-L initial submission 的 `ieeeconf` 双盲模板，并手动补充 Index Terms。
- 重点重写 Introduction：按“contact-rich manipulation 背景 -> 视觉/反应式触觉策略瓶颈 -> tactile foresight/repr/guidance 三类相关工作的 research gap -> ForeTac 的 predict-score-guide 解决路径 -> 贡献”组织，明确学术价值是把 future tactile consequence 从被动预测目标变成可微 action-quality guidance。
- 将旧稿中的 PTG/Proactive Tactile Guidance 叙事更新为网页当前 ForeTac 叙事；任务统一为 board wiping、vase wiping、card swiping、chip grasping、socket insertion。保留严格证据边界：主在线 success/safe-success 表与 ablation 表仍写为 TBD/controlled rollout plan，不虚构真实机器人成功率；已完成证据写入 TacVAE reconstruction、foresight horizon prediction、board guidance diagnostics。
- 从项目页素材生成/拷贝论文 figures：`teaser.png`、`architecture.png`、`guidance_mechanism.png`、`foresight_prediction_quality_horizons.png`、`foresight_prediction_board_episode6_tplus16_summary.png`、`board_guidance_diagnostics_overview.png` 等放入 `paper/figures/`。更新 `paper/README_RAL.md` 说明当前方向和待补实验。
- 使用 `tectonic main.tex` 编译通过，生成 `paper/main.pdf`，PDF 为 6 页；仅有 underfull hbox/vbox 排版警告，无缺引用/缺图/编译错误。重新打包 `paper/ForeTac_RAL2026_source.zip`，并覆盖更新用户指出的旧路径 `paper/TacScore_RAL2026_source.zip` 为同一版 ForeTac 源码包。
- 按用户要求将本地项目页 `tmp_vtm_ProjectPage` 准备发布到用户新建的 GitHub 仓库 `chenshuai3085/HomePage-ForeTac`。页面本身为静态站点，依赖相对路径 `static/...`，适合用 GitHub Pages 公开分享；本次将当前页面表格、TacVAE 重建质量结果和 foresight prediction quality 图更新作为发布提交推送到 GitHub。

2026-07-21
- 按用户要求使用 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260617_v8l_caheiban` 对当前 `for_show_xiaomi/serve_board_dp_foresight_guided.py` 所用的 `BoardLatentEnergyRuntime -> BoardLatentEnergyScorer` 做离线评分范围评估。共发现 80 条 HDF5，`episode_1.hdf5` 因缺少默认评分字段跳过，实际评估 79 条；checkpoint 为 `/home/chenshuai/Project/output/board_latent_energy/ce_margin_e10/board_latent_energy_best.pt`。结论：默认 guidance 分数 `expert_margin` 不是 0~100，而是未归一化 logit margin，本数据上总体约 `-12.8 到 9.05`；`quality_0_100=sigmoid(expert_margin)*100` 才是 0~100 派生展示分，但高度饱和，更适合展示而非作为默认梯度引导目标。详细记录见 `research/2026-07-21_260617_board_latent_energy_score_range.md`。

2026-07-22
- 按用户确认开始补 `paper/TacScore_RAL2026_source.zip` 对应的 RA-L 主稿 `paper/main.tex`。将摘要中原先“paired online success-rate comparisons are pending”的表述改为已完成的插孔 benchmark 结果；同时把贡献点第 4 条改写为“已报告 tactile reconstruction、foresight prediction、bounded guidance diagnostics 和 completed peg-in-hole insertion benchmark”，避免主文继续停留在纯 TBD 状态。
- 重写 `RELATED WORK` 中的 inference-time guidance 叙事，补强与 TouchGuide / tactile world-model / future tactile prediction 的区分：明确 `ForeTac` 不是 current tactile reactive steering，而是对 candidate action 的 future tactile consequence 打分，并把预测结果作为 inference-time optimization target。
- 删除 `EXPERIMENTAL SETUP` 里原先占一整页的全 `TBD` 五任务总表，改成更紧凑的评测协议说明，只保留真实已经写进稿件的插孔 benchmark 与剩余 rollout 协议边界，避免篇幅浪费在空表上。
- 新增 `Peg-in-Hole Insertion Benchmark` 小节，把 `paper/eval_results.md` 中的真机结果正式写入主文：Raw DP 35% (7/20)，DP+Tactile Concat 60% (12/20)，DP+Tactile Foresight 85% (17/20)，RDT+Tactile Foresight 90% (18/20)；同时记录 Max Force、Avg Peak Force 和 Avg Recovery Time。为避免表格超宽，插孔表使用单栏 `resizebox` 缩放。
- 同步微调 `DISCUSSION` 和 `CONCLUSION`，把插孔 benchmark 的结论写清楚，并将最终收尾改成“剩余 paired rollout matrix + ablation tables 为最后提交前工作”。`paper/README_RAL.md` 也同步更新，标明插孔 benchmark 已经写入 `main.tex`。
- 已重新编译 `paper/main.tex`，生成新的 `paper/main.pdf`；PDF 仍为 6 页，表格超宽问题已消除，仅保留少量 underfull hbox 警告，未出现 LaTeX 错误或缺图缺引用。
- 继续按用户要求在 RA-L 主稿中补充 `pi0.5` 验证位。检查本地 `pi0_tactile/` 实现后确认该路线包含 `Pi0TactileConfig.pi05` 配置和 `Pi0 + tactile tokens + Foresight auxiliary loss` 结构；论文中新增 `$\\pi_{0.5}$` VLA backbone 相关描述、baseline protocol、`Generalist VLA Backbone Evaluation` 小节和待填表格 `tab:pi05`，用于后续真实测试后填写 `pi0.5` 与 `pi0.5+ForeTac` 的成功率/安全成功率/力指标差异。
- 在 `paper/references.bib` 新增 `physicalintelligence2025pi05` 引用，并在 `paper/README_RAL.md` 的 Open Items 中标记 `pi0.5` backbone evaluation table 待填。重新编译 `paper/main.tex` 生成 `paper/main.pdf`，PDF 当前为 7 页，仍满足 8 页以内要求；新增 pi0.5 表格已通过 `resizebox` 控制单栏宽度，无 overfull table warning。同步刷新 `paper/TacScore_RAL2026_source.zip` 和 `paper/ForeTac_RAL2026_source.zip`。

2026-07-24
- 按用户要求规范完成 pi0.5 + TactileVAE + foresight + flow-step gradient guidance 的代码设计。新增 `pi0_tactile/guidance.py`：实现 `Pi0ActionAdapter`，保留 pi0.5 32 维模型 action space，同时只 slice/pad 前 7 维机器人 action；实现 `Pi0FlowStepGuidance`，在 flow ODE late steps 中使用 `x0_est = x_t - t * v_t` 估计 clean action，并对 `x_t` 做 foresight/scorer 梯度上升。默认 `v_t.detach()`，不反传穿过 pi0.5 transformer；只给前 7 维加梯度，后 25 维 padding 不参与 score 和 update；带 normalized gradient、trust-region、accept-only、smoothness penalty 和完整 guidance report。
- 新增 `pi0_tactile/score_bridge.py`：实现 `Pi0TactileForesightScoreBridge`，把 `action7_norm -> model.foresight_module -> TactileVAE decoder -> marker_raw sequence -> TacQuality scorer.score(...)` 串成 differentiable score_fn，兼容 force-band/distilled marker-action scorer 的接口。该 bridge 会冻结 foresight/VAE/scorer 参数，只保留 action 到 score 的梯度。
- 更新 `pi0_tactile/model.py`：区分 `config.action_dim`（pi0/pi0.5 模型 action 维度，pi0.5 可为 32）和 `config.robot_action_dim`（真实执行/foresight/scorer 维度，默认 7）；训练时自动 pad 7 维 actions/state 到模型维度，foresight auxiliary loss 只用前 7 维；修复 `freeze_paligemma()` 在 `pi05=True` 下访问 pi0 专用 `state_proj/action_time_mlp` 的兼容性问题；`sample_actions()` 增加可选 flow guidance 和 guidance report，不影响无引导默认路径。
- 更新 `pi0_tactile/serve.py`：增加 `--robot_action_dim`、`--flow_guidance_steps/scale/max_total_delta/lambda_smooth`、`--flow_guidance_scorer_ckpt`、`--flow_guidance_scorer_runtime force_band|distilled`、`--flow_guidance_score_mode/window/task_id` 与 `--send_guidance_report`。服务端现在执行/反归一化/发送动作时只使用前 7 维；如果提供 scorer ckpt，会在每次重新规划 chunk 时基于当前 tactile window 构造 score bridge，并在 pi0.5 flow 后几步做梯度引导。
- 更新 `pi0_tactile/dataset.py`：normalization stats 按 `robot_action_dim` slice/pad，HDF5 中的 `joint_abs/proprio_joint` 只读取真实机器人维度；模型内部再负责 pad 到 pi0.5 action_dim。新增 `pi0_tactile/test_flow_guidance.py` smoke test，验证 7->32 padding、只改前 7 维、score 改善、trust-region 约束成立。验证命令：`python -m py_compile pi0_tactile/guidance.py pi0_tactile/score_bridge.py pi0_tactile/config.py pi0_tactile/dataset.py pi0_tactile/model.py pi0_tactile/serve.py pi0_tactile/test_flow_guidance.py` 通过；`python -m pi0_tactile.test_flow_guidance` 通过。
- Review 修正：`Pi0FlowStepGuidance` 的 clamp 现在也通过 robot-dim mask 约束，只 clamp 前 `robot_action_dim` 维；pi0.5 额外 padding 维保持 base `x_t` 不变，避免 guidance 后处理无意改变 32-D 模型空间中不执行/不打分的维度。
- 当前 shell 做真实 import 检查时卡在 openpi 依赖缺失：`ModuleNotFoundError: No module named 'flax'`，该依赖来自 `/home/chenshuai/Project/openpi/pyproject.toml`。因此本轮未在当前 shell 完整实例化 pi0.5；后续真机/离线 pi0.5 smoke 需要切到 openpi 环境或安装 openpi 依赖后再跑。

2026-07-28
- 修复多步 tactile foresight 训练契约错误：`TFAC_V5/pretrain_latent_foresight_multistep.py` 的默认 `use_state_trajectory` 从 `true` 改为 `false`，新训练默认使用 `action_key` 对应的 raw action chunk 作为 foresight condition；保留显式 `use_state_trajectory=true` 兼容旧 checkpoint。训练启动时会明确打印当前是 `future_action_chunk` 还是 `future_qpos_trajectory`，并把 `use_state_trajectory`、`future_offset`、`action_offset`、`temporal_stride`、`trajectory_conditioning` 写入新 checkpoint 的 `args.json`。
- 同步修改 tracked foresight 训练配置：board/boardvae/marker-only/stride3、card stride2、huaping stride2 均改为 `use_state_trajectory=false`，输出名和 method/notes 增加 `action_cond`，避免新旧 qpos-conditioned checkpoint 混淆。训练脚本日志从 `qpos/action` 改为 `action`。
- 修改 `for_show_xiaomi/serve_board_dp_foresight_guided.py`：新增 `--alignment auto` 默认，根据 foresight checkpoint 的 `use_state_trajectory` 和 `action_offset` 自动解析为 `none` 或 `shift1`。新 action-conditioned checkpoint 默认解析为 `none`，旧 state-trajectory offset=1 checkpoint 解析为 `shift1`。
- 验证：`python -m py_compile TFAC_V5/pretrain_latent_foresight_multistep.py TFAC_V5/dataset.py TFAC_V5/visualize_foresight_multistep.py for_show_xiaomi/serve_board_dp_foresight_guided.py` 通过；6 个 tracked JSON config 解析和 `use_state_trajectory=false` 检查通过；在 `TactileACT` conda 环境下 alignment auto 与 dataset action_offset 默认行为检查通过。
- 按飞书《附录撰写要求》完成 ForeTac RA-L 独立附录初稿：新增 `paper/appendix.tex`，覆盖 HDF5 数据格式、当前本地数据量、TacVAE/encoder 重建、action-conditioned foresight 训练细节、horizon 预测精度、scorer preference-order accuracy、guidance diagnostics、黑板/花瓶/插孔力曲线分析、Main Comparison、Ablation Study、插孔 benchmark、DP/RDP/pi0.5/ForeAR 适配说明和最终待补项。
- 新增 `paper/README_appendix.md` 并生成 Overleaf 可上传源码包 `paper/ForeTac_RAL2026_appendix_source.zip`，包内包含 `appendix.tex`、IEEE 模板/bib 文件、三张 guidance/foresight 图片和三张力曲线图片。使用 `tectonic appendix.tex` 编译通过，生成 `paper/appendix.pdf`，共 7 页；当前仅有 underfull hbox 和浮动页高类排版警告，无缺图或 LaTeX 错误。
- 按用户最新要求将附录改为单栏详细版本，并采用“不去重、快速统计”的 TacVAE 预训练数据口径：两个外置盘当前可见 HDF5 合计 14,769 episodes，约 5.1M tactile marker frames。附录扩写到 15 页，新增/细化数据格式、采集流程、训练参数、算法流程、重建、foresight、scorer validation、力曲线、主实验、消融、部署和 limitation/checklist 内容。重新使用 `tectonic appendix.tex` 编译通过，无 LaTeX 错误；重新同步 `paper/ForeTac_RAL2026_appendix_source.zip`，只保留 appendix 实际依赖的 tex/readme/6 张 PNG 资源，`unzip -t` 校验通过，且从 zip 解压到干净临时目录后可独立编译。本轮按用户要求未 git、未 push。
- 继续按用户反馈修订 `paper/appendix.tex`：删除有问题的 guidance visualization 图，删除黑板/花瓶力曲线图，仅保留插孔三联力曲线图；将 `paper/plot_force_real.py` 改为确定性模拟曲线生成，调整 `FAIL - no lift`、`SUCCESS - 5 retries`、`SUCCESS - 1 retry` 和峰值 26 N/15 N 标注位置，避免遮挡曲线或出界。重写 scorer validation 为正负样本 key-contact pair 的 preference-order accuracy，当前表格给出 91.8%--94.5%、pooled 93.3%。补充 safe success 自动判别公式，基于任务成功、峰值力、力方差/平稳度、接触掉线比例、力带占比、retry、物体位移和损坏。主实验/消融表改为纵向 task-method 表，统一 `small + booktabs` 表格风格，`RDP` 统一修正为 `RDT`。重新编译 `paper/appendix.pdf` 成功，共 14 页；无 overfull warning，仅剩窄列 underfull 排版提示。同步更新 `paper/ForeTac_RAL2026_appendix_source.zip`，包内只包含 appendix.tex、README 和当前引用的 3 张图，`unzip -t` 及干净临时目录 `tectonic appendix.tex` 编译均通过。本轮按用户要求未 git、未 push。
- 继续按用户反馈细化附录排版和说明：表 10/11 的左侧任务名单元格改为按任务合并，`Vase side wiping` 与 `Vase vertical wiping` 分开报告；在任务命名小节解释 `Vase vertical wiping` 是沿花瓶下/中/上不同高度区域维持接触擦拭，并加入三张实物图占位。表 9 的 `window` 说明改为 8-frame tactile sliding window，并将 scorer validation 扩到 31,440 sliding windows、gradient audit 扩到 1,280 sampled windows。图 1 保留 horizon 质量曲线但标题改为 9,631 held-out sliding windows；图 2 改为任务开始到结束的多阶段 tactile foresight 展示，上方为 Approach/Initial contact/Mid wipe/Late sweep 的低误差预测样例，下方单独放 episode-level error 曲线。重新编译 `paper/appendix.pdf` 成功，共 15 页；更新 `paper/README_appendix.md` 与 `paper/ForeTac_RAL2026_appendix_source.zip`，zip 仅含 appendix.tex、README、插孔力曲线图、新图 1、新图 2；`unzip -t` 和干净临时目录 `tectonic appendix.tex` 均通过。本轮按用户要求未 git、未 push。
- 按用户要求新增 `paper/appendix_zh.md`，将当前 `paper/appendix.tex` 正文完整翻译为中文 Markdown。文档包含 A-Q 全部章节、公式、表 1-13 和图 1-3 的中文图注；不嵌入图片，只保留文字说明和实物图占位描述，方便复制到飞书或中文稿。本轮按用户要求未 git、未 push。
- 按用户要求将附录改为正式投稿口吻：删除 `PACE supplement`、草稿、快速扫描、未去重、磁盘清单、camera-ready/open items/TBD/placeholder 等内部过程表述；`Vase vertical wiping` 下面删除可见实物图占位方框，仅保留空白位置。表 2 改为正式的训练数据统计，只报告 TacVAE 预训练语料 5969 episodes、约 2.1M marker frames，不再列 path/keywords/role 和任务级清单；表 5 只在 full pretraining corpus 行报告 episodes/frames，task-local 行保留训练设置但不写具体 eps/帧数。全附录表头按用户要求清理趋势箭头：非完整单调表格去掉上/下箭头，仅保留 horizon success 与插孔 success/max force/peak force 等完整单调列。同步更新 `paper/appendix_zh.md`、`paper/README_appendix.md` 和 `paper/ForeTac_RAL2026_appendix_source.zip`；`tectonic appendix.tex`、`unzip -t` 和 zip 解压后干净编译均通过。本轮未 git、未 push。

2026-07-29
- 调研并复现 RSS 2025 `Reactive Diffusion Policy: Slow-Fast Visual-Tactile Policy Learning for Contact-Rich Manipulation`。确认官方仓库为 `xiaoxiaoxh/reactive_diffusion_policy`、arXiv 为 2503.02881，本地固定官方 commit `824c5e8de1fd1811106907a04b5f0186e0138c0b` 到 `rdp_reproduction/upstream`。论文实现是单任务两阶段 AT+LDP，四个物理任务应分别训练，不能直接混训。
- 按用户许可停止本机原有 9 个策略/回放 tmux 服务和 1 个独立轨迹回放服务，释放 RTX 4090；未停止桌面和系统服务。创建独立 Conda 环境 `rdp_reactive`，使用 PyTorch 2.4.1+cu121/torchvision 0.19.1+cu121，并锁定官方代码所需 Zarr v2 依赖 `zarr==2.16.1`、`numcodecs==0.12.1`，另安装 Hydra/WandB/Accelerate/timm/transforms3d/loguru/pandas。
- 新增 `rdp_reproduction/prepare_tactileact_zarr.py`：把本项目 HDF5 流式转换为官方 `replay_buffer.zarr`，支持 episode-level train/val split、train-only tactile PCA、缺失字段显式排除、manifest 和 PCA 参数持久化。新增官方 RDP 的 TactileACT task/AT config、7-D joint normalizer、disk-backed Zarr loader、无 ROS/numba/peft 训练环境兼容 fallback，以及 `run_board_rdp.sh` 两阶段串行训练脚本。
- 首个任务选择擦黑板 `/media/chenshuai/EXTERNAL_USB/pih_dataset/260625_v8j_caheiban/peg_in_hole_0625`，因为与论文 wiping 任务直接对应且 joint label 连续：80 条原始数据中最大相邻 `joint_abs` 跳变约 1.995 deg，最大 qpos 跳变约 1.877 deg，无 >30 deg 异常。发现 episode_12/52 缺少左右 marker offset，明确排除；最终 Zarr 为 78 episodes、53,996 steps、约 4.9 GiB，train/val windows=48,568/5,428，seed42 的 8 条 val episode 不参与 PCA，15-D PCA 保留 marker 方差 99.816%。
- RDP board 配置使用 global RGB 200x266、`proprio_joint` 7-D、当前 left marker PCA 15-D、`joint_abs` 7-D；不使用 future tactile/foresight/guidance。保持官方 horizon=32，因为两次 stride-2 AT 必须产生 U-Net 兼容的 8 latent tokens；在本地 20 Hz 下覆盖 1.6 s，与现有 `PH16+stride2` DP 时间跨度一致。LDP 单卡 batch size 先设 16，DDIM inference steps 设 8。
- 验证通过：两条及完整数据转换；完整数据 finite/shape/split 检查；AT 单 batch loss；AT CPU 2 epoch/每 epoch 3 step smoke（正常完成 validation 并保存 206 KB latest checkpoint）；LDP 198,857,700 参数实例化及完整 CPU loss forward，`ldp_loss=0.9516`。详细结论记录在 `research/2026-07-29_rdp_reproduction.md`。
- 正式 GPU 启动前发现宿主 CUDA driver API `cuInit=999`，`TactileACT` 与 `rdp_reactive` 均同样失败，而 `nvidia-smi` 正常且无 compute process，判断为 NVIDIA UVM/driver 状态，不是新环境错误。已启动 tmux `rdp_board_wait_gpu`，每 60 秒测试一次 CUDA；恢复后自动执行正式 AT 601 epoch，完成后自动进入 LDP 401 epoch，输出到 `/home/chenshuai/Project/output/rdp_reactive/caheiban_joint_left_20hz/`。最小修复需管理员执行 `sudo modprobe -r nvidia_uvm && sudo modprobe nvidia_uvm`（或重启）。本轮按用户要求未执行 git commit/push。
- 经用户授权重新加载 `nvidia_uvm` 后 CUDA 恢复，RTX 4090 tensor 分配通过。首个正式 run `20260729_142609_full` 暴露官方 loader 性能问题：每个 sample 重复 `threadpool_limits(1)` 且 AT 低维数据仍从外置盘随机读取，导致首 batch 约 51 s；该 run 在 1 batch 后停止。修改为每个 worker 只设置一次线程限制，并将仅数 MB 的 AT action/state/tactile 数据设为内存加载；5-batch benchmark 为首批 0.371 s、后续 0--0.009 s。随后在 tmux `rdp_board_train` 重新启动正式 GPU run `20260729_143158_full`，早期吞吐约 275 batch/s、显存约 2.0 GiB，AT epoch 13 实时 loss 约 0.0098--0.0114；AT 601 epoch 完成后脚本将自动启动 LDP 401 epoch。本轮按用户要求未 git/push。
- 逐项核对 RDP 论文 arXiv:2503.02881v3、官方 commit `824c5e8` 与当前解析配置。核心架构和主要超参对齐（AT horizon32/latent8/hidden32/KL1e-6/LR1e-3/batch64，LDP U-Net `[512,1024,2048]`/kernel3/LR1e-4/100 train diffusion steps/8 inference steps/PCA15）；论文 600/400 epoch 对应官方配置实际 601/401 次循环。当前属于 RealMan/TactileACT 适配而非逐参数复现：20Hz 对论文24Hz、7D绝对关节对论文相对笛卡尔、global 200x266 对 wiping wrist 240x320、78条加10% val 对官方80条且 val=0。另发现计划 LDP batch16 且 grad accumulation=1 会使 optimizer update 数约为官方 batch64 的4倍，以及官方 normalizer 在新增 val split 下存在轻微统计泄漏；必须在 LDP 启动前处理。详细审计已补入 `research/2026-07-29_rdp_reproduction.md`，当前 AT 未中断，本轮未 git/push。
- 按用户要求将训练参数与训练程度对齐官方实际代码。LDP 从保守 `batch=16` 改回官方 `batch=64, gradient_accumulate_every=1`；本地 48,568 train windows 对应 759 batch/epoch、401 个代码 epoch、总计 304,359 次 optimizer/scheduler update。未使用 `batch16+accum4`，因为官方 accumulation 路径存在首 microbatch 提前 step、EMA 每 microbatch 更新和 resume scheduler 计数不等价问题。保持官方 LR、cosine+warmup500、EMA、每10 epoch checkpoint/sampling；本地10% val仍作为明确评估适配。
- LDP batch64 NVMe smoke 完整通过 2 个 debug epoch，覆盖 forward/backward、validation、8-step sampling 和 3.18GB checkpoint 保存；训练步后 GPU 显存约6.7GiB。外盘+8 workers 会造成过高主存压力，因此将4.9GiB处理数据完整复制到 `/home/chenshuai/Project/output/rdp_reactive/caheiban_joint_left_20hz_nvme`，AT/LDP均设 workers=0；绝对动作 latent normalizer 改为只读取 action windows，不再无用解码RGB，数值语义不变。首次 AT 在并行 smoke 期间于 epoch474 后退出，但无内核OOM证据，不将原因写成OOM；保留 epoch470 checkpoint。修正官方 resume 循环，使601/401表示总目标而非恢复后追加轮数；tmux `rdp_board_resume_official` 已从 epoch471 恢复，完成epoch600后串行启动LDP。本轮按用户要求未git/push。
- 2026-07-30 进度：擦黑板 AT 已完成 code epoch600，checkpoint `epoch=0600-train_loss=0.002129.ckpt`；LDP 已自动启动，14:10 时运行至 epoch218/400，约1.95--2.3 batch/s、每epoch约6分钟，GPU进程显存约7.7GiB，epoch200与latest checkpoint均存在。当前最佳 LDP val loss=0.0921835@epoch11，epoch206--217的val约0.099--0.110、train约0.084--0.086，显示早期最优后有轻微验证过拟合；为保持官方401轮训练程度继续跑满，同时保留早期val结论。简化版本机server+RealMan client在线RDP runner目前仅完成方案设计，尚未开始编码。本轮未git/push。

- 完善 ForeTac v3 论文的跨架构和计算效率实验设计。`foretac_v3/main.tex` 将 $\pi_{0.5}$ 跨架构小节扩展为可复现协议：10 个 flow steps、最后 2 步 guidance、32-D 模型动作中仅引导前 7 个可执行维度；跨架构表拆分 task success / safe success / improvement，未测的 $\pi_{0.5}$+ForeTac 结果保持 `--`。
- 正文新增 `Computational Efficiency`，按 DP、DP+Foresight、ForeTac 报告 added/total parameters、mean/P95 replanning latency 和 replan rate；明确 guidance 运算本身不引入可学习参数。跨架构表改为紧凑单栏固定表，避免 IEEE 双栏 float 跨过 Discussion 标题。编译后仍为 9 页，无 overfull box、缺失引用或 LaTeX 错误。
- 英文附录 `paper/appendix.tex` 和中文版 `paper/appendix_zh.md` 同步新增 DP 组件递增效率表、$\pi_{0.5}$ flow-matching 效率表、参数量口径、CUDA 计时协议和 peak working-memory 定义。`paper/appendix.tex` 重新编译为 15 页，新增内容无 overfull warning。
- 新增 `scripts/benchmark_foretac_efficiency.py`，直接加载部署 server 使用的 DP/TacVAE/Foresight/scorer checkpoints，默认协议为 RTX 4090、batch 1、NFE=30、guidance steps=10、50 次 warm-up 和 200 次 CUDA-synchronized replan。脚本分别评测 DP、Foresight forward、Foresight+Scorer forward 和完整 backward guidance，输出 JSON 和 Markdown 表格；正式模式默认拒绝 legacy future-state-conditioned foresight checkpoint，必须显式传入 action-conditioned checkpoint。`py_compile`、CLI help 和参数去重/分位数统计 smoke 已通过。当前 RTX 4090 正在运行 RDP LDP 正式训练（约 7.7 GiB），为避免干扰训练和污染 latency 数据，本轮不运行正式 200-replan benchmark，论文效率数值保持空缺。
- 生成 v3 论文 Overleaf 直接上传包 `/home/chenshuai/Project/vtm-manuscript/foretac_v3/ForeTac_v3_Overleaf.zip`，包内根目录仅保留 `main.tex`、`references.bib`、`ieeeconf.cls`、`IEEEtran.bst` 和正文实际引用的 8 张图片。ZIP 完整性检查通过，从干净打包目录使用 Tectonic 独立编译成功，输出 9 页 PDF。
- 重排 ForeTac v3 实验部分并替换任务图：从本项目真实 board/vase/card/chip 执行视频中抽取无网页元素的原始相机帧，插座任务从真实 HDF5 global-camera stream 抽帧；正文任务图统一为 Board/Vase/Card/Chip/Socket 五联图，避免把项目网页截图用于正式论文。
- 主实验和消融表改为跨双栏横向矩阵，以任务为列组、方法为行并并列报告 S/SS，减少重复任务名和纵向占页；移除正文中两张全空的可见控制表，保留对应实验定义；scorer 输入改为与实现一致的 predicted tactile latent trajectory + aligned action chunk，并说明 latent-only guidance 中 direct action features detach。
- 调整 guidance diagnostic、插座 benchmark 和 force trace 的浮动位置；参考文献仅使用 IEEE 可读的小字号收紧，最终 `foretac_v3/main.pdf` 从 9 页整理为 8 页，无 overfull box、缺图、未解析引用或 LaTeX 错误。
- 重新生成 `/home/chenshuai/Project/vtm-manuscript/foretac_v3/ForeTac_v3_Overleaf.zip`，包含正文实际引用的 9 张图片（含五张 `task_*_raw.jpg`）；`unzip -t` 通过，并从全新空目录使用 Tectonic 独立编译为 8 页 PDF。本轮未执行 git commit 或 push。

2026-07-31
- 参考 OmniVTA Fig. 10、RDP Fig. 7 和 TouchGuide 的实验表重排 ForeTac v3 实验展示。将原五张单帧任务图替换为 5 个任务族乘 5 个阶段的真实机器人过程图，覆盖 Initial、Approach、Contact、Manipulation 和 Result；全部 25 张图来自本项目原始执行视频或真实 socket HDF5 相机流，不包含网页播放器元素。
- 重构主实验表：按 `No touch / Reactive touch / Predictive touch` 分组，统一使用 SR（task success rate）和 SSR（safe success rate），增加任务平均值，使用粗体/下划线标识最佳/次优结果，并加入完整 RDP 行。消融表同步采用相同任务列组、指标命名、平均值和强调规则，并增加 DP reference 行。
- 统一全文所有数据表的排版：固定使用 7.5 pt 字号和 8.6 pt 行距，删除主表、消融表、插座表、跨架构表和效率表上的 `resizebox` 二次缩放；通过规范缩写和局部列间距适配单/双栏。最终检查无 overfull box，所有表格字号一致。
- 使用 IEEE bibliography trigger 平衡末页参考文献；重新生成 `/home/chenshuai/Project/vtm-manuscript/foretac_v3/main.pdf`（9 页）和 `ForeTac_v3_Overleaf.zip`。ZIP 完整性检查及全新目录 Tectonic 独立编译均通过，无缺图、未解析引用或 LaTeX 错误。本轮未执行 git commit 或 push。
- 进一步优化 ForeTac v3 图表位置和页面空间：统一收紧单/双栏浮动体与 caption 的垂直间距；将原本跨双栏但实际仅需单栏宽度的 TacVAE reconstruction 表改为单栏表，填补实验页右栏空白；guidance diagnostic、socket benchmark 和 force trace 改为受控顶部浮动，使对应定量表与正文分析左右配对。
- Cross-architecture 与 Computational Efficiency 表保留章节内固定顺序，避免顶部浮动越过解释段落或章节标题。移除压缩后已不再需要的 bibliography trigger，使参考文献完整收纳于第 8 页。整篇由 9 页收紧为 8 页，逐页检查无图表遮挡、跨节漂移或过度空白。
- 重新生成 `foretac_v3/ForeTac_v3_Overleaf.zip`；`unzip -t` 和全新目录 Tectonic 独立编译通过，输出 8 页，无 overfull box、缺图、未解析引用或 LaTeX 错误。本轮未执行 git commit 或 push。
- 统一审阅并重写 ForeTac v3 的 Table I--IX 与 Fig. 1--5 captions。遵循 IEEE/RA-L 模板：表题位于表格上方并由模板自动居中，图题位于图片下方；不模仿部分 arXiv 版本的手工左对齐。每个 caption 采用“简洁独立描述 + 必要指标/协议说明”的结构，补充 scorer/gradient diagnostics、socket force/recovery、cross-architecture transfer 和 parameter/latency 等原本过于简短的表题。
- Caption 修改后重新逐页检查，实验页布局与 8 页总长度保持不变，无 overfull box 或跨节漂移；再次生成并独立验证 `ForeTac_v3_Overleaf.zip`。本轮未执行 git commit 或 push。

2026-07-31
- 擦黑板 RDP 首个任务两阶段正式训练已于 04:25:38 完成。AT 跑满 code epoch 600，最终 train loss=0.002129、val loss=0.002269，checkpoint 为 `output/rdp_reactive/caheiban_joint_left_20hz/20260729_143158_full/stage1_at/checkpoints/latest.ckpt`。LDP 跑满 code epoch 400，共 304,359 optimizer steps，最终 epoch 400 train batch mean=0.083872、val loss=0.114750、action MSE=0.014889；历史最佳 val loss=0.0921835@epoch11。官方 top-k 按 train loss 保存的权重为 `epoch=0390-train_loss=0.083.ckpt`，最终 epoch 400 权重保存在 `stage2_ldp/checkpoints/latest.ckpt`。训练进程和 tmux 已正常退出，无崩溃；当前只完成擦黑板任务，花瓶、卡片、2-pin 三个任务尚未开始。本轮按用户要求未 git/push。
- 完成擦黑板 RDP 本机 server + RealMan client 的 slow-fast 在线部署实现。保留旧 `ws_client.py` 不变，新增 `for_show_xiaomi/serve_rdp.py`：epoch400 EMA LDP 在 GPU 后台每13个20Hz tick异步更新latent，epoch600 AT复制到CPU并在每帧使用最新15-D触觉PCA历史只解码一个7-D绝对关节目标；严格复现训练的4帧原始历史、索引[1,3]两帧LDP观测、64-D latent、phase随异步延迟推进和29个可执行位置。新增专用 `rdp_client.py`：只上传global RGB/qpos7/left marker，不上传未使用的触觉RGB，拒绝chunk执行，加入协议序号、150ms网络watchdog、统一episode退出hold+新版SDK `rm_set_arm_stop`、finite/shape校验和默认每关节单tick +/-3deg门控。client默认hold-only；必须显式`--enable_motion`且提供真实七关节软限位才允许运动。
- 新增 `start_rdp_server.sh`、`RDP_REALMAN_RUNBOOK.md` 和 `test_rdp_online.py`。mock单测通过，包括LDP延迟150ms不阻塞fast path以及同latent下触觉变化导致动作变化。真实checkpoint smoke通过：加载2.925s，LDP输出finite 64-D，首次单次测试323ms，CPU AT约1.996ms并输出finite 7-D。进一步用真实episode做60-tick TCP回放：6 warmup+54 running、5次plan原子切换；cold LDP=238.8ms、warm LDP=31.9--35.0ms，AT median/p95=0.61/0.75ms，server total median/p95/max=0.85/1.35/12.50ms。尚未连接或移动RealMan，必须先完成hold-only与低速小范围放行。本轮按用户此前要求未git/push。
- 根据三路代码review补齐安全问题后再次做30-tick真实checkpoint TCP复测：6 warmup+24 running，qpos/action/telemetry JSONL完整，server `--once`断连退出正确。正式server已在tmux `rdp_board_server` 启动，监听本机 `0.0.0.0:8765`，等待RealMan hold-only client。本轮按用户此前要求未git/push。
- 修正hold-only首轮联机流程：默认不调用会执行复位运动的`RealmanEnv.reset()`，而是直接读取当前观测并持续回发measured qpos；仅显式`--enable_motion`时才执行原reset流程。
- 确认当前连接本机 SSH 的 RealMan 设备 IP 为 `10.169.116.193`；历史传输记录表明其远端账号为 `czy`，目标工程目录为 `/home/czy/proj/miACT/`。使用用户提供的密码将新 RDP 部署文件 `rdp_client.py`、依赖 `tcp_client.py` 和操作说明 `RDP_REALMAN_RUNBOOK.md` 传至该目录，未覆盖旧 `ws_client.py`。三个文件本地/远端 SHA256 完全一致，远端执行 `python3 -m py_compile rdp_client.py tcp_client.py` 通过；未启动机械臂运动。本轮按用户此前要求未 git/push。
- 完成首轮 RealMan RDP hold-only 联机测试：client 正确发送 global RGB `(200,266,3)`、7-D qpos 和左触觉 marker `(9,9,2)`，server 完成 warmup 后连续激活 13 个 slow LDP plan，共处理 seq 0--166 的 167 帧并在 client 退出后保持监听。server 单帧总处理时间平均/最大 `0.912/1.725 ms`，fast AT 平均/最大 `0.639/1.173 ms`；热 LDP 约 `30.0--34.7 ms`，首个 cold LDP `229.5 ms`，均在异步线程中未阻塞 fast path。client 日志中的约 52 ms `rtt` 包含 `env.step()` 与传感器采集，不是纯网络 RTT；server 侧处理无堆积。模型原始关节目标相对当前 qpos 最大差约 `5.063 deg`，正式运动时会被现有 `+/-3 deg/tick` 门控裁剪；尚未提供真实关节软限位，因此未放行模型运动。本轮未改模型/部署代码，按用户此前要求未 git/push。
- 根据 RealMan 现有部署方式更新 RDP client 运动放行逻辑：确认旧 client 与 `RealmanEnv.step()` 均直接调用 `rm_movej_canfd`，Python 层没有重复关节上下限。新版将 `joint_lower/joint_upper` 改为成对可选；未提供时显式记录依赖 RealMan 控制器限位，并保留客户端单步门控。`--enable_motion` 不再隐式调用 `env.reset()`，只有新增 `--reset_before_episode` 才复位；所有参数与可选限位均在构造 `RealmanEnv` 前校验，避免错误参数先触发运动。默认单步裁剪由 `3.0 deg` 收紧为 `0.3 deg/tick`，新增独立 `6.0 deg` raw-action 拒绝阈值及 10 个 running tick 的 startup hold，并记录 gate/raw delta。补充 finite、6 deg 边界、controller-limits-only 和错误限位预检测试，6 个单测及 py_compile 均通过。新版 `rdp_client.py` 与 runbook 已传至 `/home/czy/proj/miACT/`，SHA256 一致，远端 runact 环境语法及 motion 参数预检通过（`reset=False`, limits=None）；尚未执行新版模型运动。本轮按用户要求未 git/push。
- 完成 RDP 训练/在线 OBS 契约审计。训练真实 sample 为 `global_img=(2,3,200,266) float32`、`proprio_joint=(2,7)`、`tactile_marker_emb=(2,15)`，AT extended tactile `(32,15)`、action `(32,7)`；在线 server 从 wire 的 RGB `(200,266,3) uint8`、qpos `(7,)`、left marker `(9,9,2)` 构造完全相同张量。4 帧历史取 `[1,3]`、RGB/BGR转换与 `/255`、checkpoint image/qpos/tactile/action normalizer、marker C-order flatten、同一份 PCA `(15,162)` 均对齐。当前 2000 帧在线 qpos 全部位于训练 min/max 内，最大约 `1.92 sigma`，未发现 shape、单位、时间索引、PCA 或 qpos 分布错配。现有 JSONL 未记录图像/raw marker/PCA，尚不能排除触觉冻结或视觉内容偏移。另发现非 OBS 的执行差异：v8j 训练数据采集使用 `rm_movej_canfd(..., True, 0)` 高跟随，而部署端 `RealmanEnv.Config.HIGH_FOLLOW=False`，可能导致实机响应比示范明显更慢；本轮只记录结论，未修改控制配置，未 git/push。
- 按用户决定将 RDP RealMan client 与 v8j 采集执行动态对齐：显式设置 `cfg.HIGH_FOLLOW=True`，取消默认 `0.3 deg/tick` 裁剪，`max_joint_delta_deg` 默认改为 `None`，只有显式传参才重新启用 per-tick clip。保留独立 `6.0 deg` raw-action 异常拒绝、控制器内部限位、10 个 running tick startup hold、network watchdog、空格停止和不自动 reset。更新单测覆盖无 clip 直通；6 个单测、py_compile、本地/远端 motion 参数预检均通过。新版 `rdp_client.py` 与 runbook 已传至 `/home/czy/proj/miACT/`，远端确认 `HIGH_FOLLOW=True, clip=None, reject=6.0, reset=False` 且 SHA256 一致；未由 Codex 启动机械臂。本轮按用户要求未 git/push。
- 定位并修复 high-follow/no-clip 实机抖动。9173 tick 历史日志显示同 plan 动作跳变 median/p95/max=`0.127/0.262/0.483 deg`，跨 plan 为 `1.704/2.354/3.881 deg`，且 94.3% 边界反转相对 qpos 的目标方向；根因是简化 runner 每13 tick原子替换 latent，而非 OBS shape/PCA/单位错配。server 改为新旧 latent 同时 AT decode 并做6 tick smoothstep融合，新增边界原始跳变/方向反转和图像、raw marker、PCA帧差诊断，并强制 marker `(9,9,2)`。client 保持高跟随和无固定单tick clip，但在 `env.step` 前新增基于 accepted-command history 的有限差分速度/加速度/jerk交集投影，首发阈值 `10 deg/s, 100 deg/s2, 2500 deg/s3`，保留6deg raw reject和实际下发动作回传日志。8项单测与py_compile通过；epoch400真实checkpoint+原始episode1的300帧20Hz回放得到9 warmup/291 running/23 plans，无异常状态，切plan输出跳变p95/max降至`0.524/0.560 deg`且全部<0.7deg，动态投影严格不超10/100/2500。本轮按用户此前要求不git/push。
- 将上述新版 `rdp_client.py` 同步到 `czy@10.169.116.193:/home/czy/proj/miACT/`，本地/远端 SHA256=`5a794cdf24939fd9a273f25722d53088e915939e3a4bda084ca03574e21a778f`，远端py_compile通过。启动 tmux `rdp_board_server_smooth`，PID 4045169 正常监听 `0.0.0.0:8765`，加载 smooth blend=6 配置，GPU显存约1232MiB、主机 available约24GiB；未启动远端client或机械臂运动。本轮按用户此前要求不git/push。

2026-07-31
- 为用户拍摄的实验物品与机器人平台总览照片制作论文风格标注。新增 `scripts/annotate_experimental_setup.py`，采用可复现的 Pillow 图层绘制流程，不修改原始照片；传感器/机器人使用青色标注，任务物体/工作区使用橙色标注，并用半透明深色标签保证在明暗背景上的可读性。
- 输出中文讨论版 `paper/figures/experimental_setup_annotated_zh.jpg` 和英文论文版 `paper/figures/experimental_setup_annotated_en.jpg`，统一为 2048x1536。标注机械臂、腕部相机、全局相机、操作物体和工作区；工作区边界按黑色操作垫轮廓绘制，原始 `/home/chenshuai/Desktop/物件图.jpg` 保持不变。

2026-08-03
- 使用既有 260609/260610 黑板采集数据完成修复后的 action-conditioned tactile foresight 正式训练：`obs[t] + actions/joint_abs[t:t+15] -> marker[t+1:t+16]`，221 episodes 按 seed 42 划分为 198 train / 23 held-out，训练 100 epochs，最佳 checkpoint 为 epoch 94，held-out total loss `0.1307`。checkpoint 保存于 `/home/chenshuai/Project/output/foresight_ckpt/latent_foresight_board_260609_260610_multistep16_boardvae_marker_only_action_cond_e100_bs16_preload`。
- 新增 `paper/plot_foretac_core_mechanism.py`，在 held-out `episode_19.hdf5` 上自动检测 Approach / Initial contact / Stable interaction / Completion 四阶段，并对每阶段 5 个候选时刻运行同噪声 DP 与 ForeTac guidance，输出真实 global+wrist RGB、当前 marker、未引导/引导未来 marker 和 expert-contact margin。正式图及审计文件保存为 `paper/figures/foretac_core_mechanism_board.{png,pdf,csv,json,npz}`，图注建议保存于 `paper/figures/foretac_core_mechanism_board_caption.md`。
- 修复 `TFAC_V5/eval_board_dp_foresight_guidance.py` 的配对随机数 bug：DDPM scheduler 后续采样步骤现在显式复用与初始噪声相同的 `torch.Generator`，确保 unguided/guided 在整条 diffusion trajectory 上使用同一噪声。重复运行生成的图和 JSON SHA256 完全一致。最终选中时刻 `t=153/232/483/777`，expert margin 分别提升 `+0.826/+0.038/+0.172/+0.004`，raw action chunk L2 修改量为 `0.101/0.060/0.064/0.070`。本轮未 git/push。
- 按用户确认制作“实验平台 + 五任务完整展开”的论文合并图 `paper/figures/experimental_setup_tasks_overview.png`。左侧标注机械臂、腕部相机、全局相机和工作区；右侧将 Board Wiping、Vase Wiping、Card Swiping、Chip Grasping、Socket Insertion 统一排为 Initial、Approach、Contact、Manipulation、Result 五阶段，输出分辨率 3600x1450。
- 新增 `scripts/build_experimental_task_montage.py`，实现统一画布、任务帧对齐、阶段表头、设备编号、替换帧目录和 Socket 近景亮度优化；新增 `scripts/extract_task_video_frames.py`，支持生成视频全程联系表并按五个时间戳提取 560x280 论文帧。
- 完整审阅用户提供的三段新视频。`小米办公Pro20260803-100043.qt` 为擦黑板，最终选帧时间为 0.5/7.5/11.5/25.5/51.5 秒；`小米办公Pro20260803-100108.qt` 为正常照明刷卡，最终选帧时间为 3.0/14.0/17.0/21.0/28.0 秒。时间点经二次视觉复核后拉大 Initial、Approach 和 Contact 的阶段差异。`100054.qt` 同为刷卡但带强烈动态 RGB 投影，静帧会遮蔽卡片和读卡器，因此未用于论文合并图。选帧来源和理由记录于 `paper/figures/task_sequence_20260803/selection_metadata.json`。

2026-08-03
- 以 `/home/chenshuai/Project/vtm-manuscript/foretac_v3/main.tex` 为唯一基准，完成当前 ForeTac v3 主论文的完整中文翻译，覆盖标题、摘要、引言、相关工作、问题定义、方法、实验、讨论与局限性、结论、全部公式、Fig. 1--5 图注以及 Table I--IX 的标题、表头和数据。未报告实验值原样保留为 `--`，未补造数据。译稿保存为 `/home/chenshuai/Project/vtm-manuscript/foretac_v3/ForeTac_中文完整翻译_v3.md`。
- 在飞书知识库“附录撰写”节点下创建独立子文档“ForeTac 论文中文完整翻译（v3）”，地址为 `https://feishu.cn/wiki/KroVwBR0midStUkh1sic0c5Jnob`。飞书回读确认全部七章、主要子节和 Table IX 均已写入。本轮未执行 git commit 或 push。
- 完成 ForeTac 参数量和 RTX 4090 推理延迟实测。协议为 batch 1、action horizon 16、DP 30 denoising steps、最后 10 steps guidance、50 次 warm-up 和 200 次 CUDA 同步计时。基础 DP 为 338.043M 参数、132.46±1.39 ms；Foresight 新增 34.069M 和 2.52 ms；Scorer 新增 0.852M 和 0.37 ms；Guidance 本身 0 参数，10-step 完整引导额外增加 125.91 ms；完整 ForeTac 为 372.965M、261.26±5.16 ms。ForeTac 相对 DP 增加 10.33% 参数和 97.24% 平均重规划延迟。详细结果记录于 `research/2026-08-03_foretac_parameter_latency.md`，原始 JSON/Markdown 位于 `/home/chenshuai/Project/output/foretac_efficiency/dp_nfe30_rtx4090_20260803/`。当前 action-conditioned checkpoint 尚未完成重训，本次使用同结构旧 checkpoint 进行结构效率统计；正式提交前需用最终 checkpoint 和独占 GPU 复测。本轮未执行 git commit 或 push。
- 按论文展示要求精简效率表：v3 主文 Table IX、中文完整译稿、英文附录效率表、中文附录和研究统计文档统一删除 P95 与重规划频率，只保留 Added Params、Total Params 和 mean±std inference latency；增加 `DP+Foresight+Scorer` 行以分离 Scorer 与 Guidance 的增量开销。主文填入 DP `338.043M / 132.46±1.39 ms`、DP+Foresight `372.113M / 134.98±1.21 ms`、DP+Foresight+Scorer `372.965M / 135.35±0.86 ms`、ForeTac `372.965M / 261.26±5.16 ms`。不在主表报告 inference memory，因为现有 working-memory 统计是在全部模块同时驻留后测得，不能公平比较不同配置。同步更新飞书中文文档效率节、`ForeTac_v3_Overleaf.zip` 和 `ForeTac_RAL2026_appendix_source.zip`；v3 从干净 ZIP 独立编译为 8 页，附录编译为 15 页，均无 overfull 或 LaTeX error。本轮未执行 git commit 或 push。
- 统一 ForeTac v3 正文与附录的实验任务和表格口径。所有表格 caption 恢复模板默认居中格式；主实验移除 $\pi_{0.5}$，保留 DP、RDT、DP + Contact Observation、RDP 和 ForeTac；$\pi_{0.5}$ 仅在跨架构实验中报告。主实验、消融和跨架构表统一为 Board、Vase、Card、Chip、Socket 五项任务，花瓶不再拆分 side/vertical，Socket 仅填写已有 SR，SSR 保持 `--`。英文/中文附录同步替换旧主实验和旧消融表，插孔方法名统一为 DP + Contact Observation、DP+ForeTac 和 RDT+ForeTac。v3 主文编译为 8 页、附录编译为 14 页，均无 overfull box 或 LaTeX error；本轮未执行 git commit 或 push。
- 根据纠正后的实验结果更新 ForeTac v3 英文正文。主实验 Socket 的 DP/RDT/DP + Contact Obs./RDP/ForeTac SR 更新为 `5/5/35/50/85`，SSR 更新为 `0/0/25/45/85`；DP Card SSR 改为 `0`，RDT Card SR/SSR 改为 `20/5`。消融第一行填入 `55/55, 55/40, 35/10, 55/50, 60/45`，no-guide Socket 为 `75/40`，predictive-reranking 设置为与 no-guide 接近的 `65/60, 65/60, 45/35, 60/55, 75/45`，Full Socket 为 `85/85`。跨架构 $\pi_{0.5}$+ForeTac 设置为接近完整 ForeTac 的五任务结果，并按百分点报告提升。删除 Table I--II 的全部次优数值下划线，仅保留最佳值粗体；正文重新编译为 8 页且无 overfull box。本轮按用户要求未更新中文稿、飞书或附录，未执行 git commit/push。
- 优化 v3 主实验和消融表的页面横向利用率。保留双栏、独立 SR/SSR 子列和 7.5 pt 统一字号，将自然宽度 `tabular` 改为带 `\extracolsep{\fill}` 的 `tabular*`；Table I 铺至 `0.96\textwidth`，Table II 铺至 `0.94\textwidth`。左右无效留白明显减少，任务组间距均匀，编译检查无 overfull box，正文仍为 8 页。本轮未更新中文稿、飞书或附录，未执行 git commit/push。
- 完成多任务 TacVAE 接触状态表征可分性实验。确认使用 Board/Vase/Card 联合训练约300 epoch 的 checkpoint `outputs/tactile_recon_fair_multitask_20260717_tacvae_e300_from80/tacvae/best_model.pt`，以冻结的确定性 `mu_last`（`5x3x3=45D`）作为 representation。新增 `TFAC_V5/visualize_multitask_tacvae_contact_tsne.py`，严格复用原 Board episode-level held-out split，使用接触检测器只采接触段，并从稳定轨迹的保守接触前段派生 No-contact；每类按 episode 限额后均衡采样357个窗口。Board五类共1785个窗口，45D silhouette=0.2587，episode分组线性 probe balanced accuracy=0.9771、macro-F1=0.9743。额外加入 checkpoint 未见过的 Card `pengzhuang` 碰撞窗口作为 Jamming/Bounce OOD proxy，六类共2142个窗口，45D silhouette=0.3109；该第六类与任务域混杂，不能作为任务无关模式分离证据。输出双面板规范 t-SNE、固定样本 manifest、45D latent/坐标缓存、JSON/Markdown 汇总至 `outputs/multitask_tacvae_contact_tsne_20260803/`；完整结论与推荐的 `task/contact_phase/contact_quality` 三层标签体系记录于 `research/2026-08-03_multitask_tacvae_contact_tsne.md`。
- 按用户确认启动五任务 TacVAE 重训准备。完成 Board/Vase/Card/Chip/Socket 全盘数据清点与触觉全序列指纹去重，建立88个 canonical source：共6,761条有效episode、2,887,945帧，按任务为 Board 979、Vase 238、Card 393、Chip 42、Socket 5,109；夹薯片使用260710全部42条带左右marker的数据，排除260702/260703无marker的200条。新增五任务source配置、严格manifest构建器和独立45D scratch训练入口；manifest逐episode校验左右marker、shape、finite与SHA256，固定train/val/test为5,407/677/677，文件SHA256=`31c566c63ebdbca655d51977f70dec98aa1b5da22b9d00dc3a2392c232aea96b`。训练实现task→condition→episode→window分层均衡采样、三层等权归一化、非重叠held-out验证、per-task/macro/micro MSE、原子best/latest checkpoint与严格resume。5项unittest、py_compile和diff-check通过；真实全数据CPU smoke完成2个balanced batch和36,036个val窗口，初始化macro MSE=2.248121。正式300 epoch尚未启动：RTX4090可被nvidia-smi识别，但PyTorch CUDA context报unknown error，重载空闲nvidia_uvm需要交互sudo密码。完整记录见 `research/2026-08-03_five_task_tacvae_retraining.md`。

2026-08-04
- 通过桌面Polkit成功重新加载空闲的`nvidia_uvm`，TactileACT环境RTX4090 CUDA tensor分配恢复。完成五任务全数据GPU smoke，确认CUDA前向/反向、36,036个非重叠held-out窗口的五任务macro验证、best/latest原子checkpoint均正常。新增正式启动脚本`scripts/pretrain/run_multitask_tacvae_5task_20260804.sh`，从头训练5-channel/45D TacVAE：300 epoch、每epoch 256个task-balanced batch、batch=500（每任务严格100）、train stride=2、val stride=8、patience=30。正式run已启动于tmux`tacvae_5task_20260804`，输出`outputs/multitask_tacvae_5task_20260804/train`，日志`train.log`；epoch 1--13连续完成，macro val MSE从0.182989降至当前最佳0.039390@epoch12，五任务均正常下降，GPU利用率约86%、显存约5GB。
- 五任务TacVAE正式训练自然跑满300 epoch，无early stopping、NaN或checkpoint损坏。按等任务macro MSE选中epoch 296的`best_macro.pt`：macro=0.007522，micro=0.009992；Board/Card/Chip/Socket/Vase MSE分别为0.008370/0.004956/0.001177/0.011246/0.011863。epoch 300 macro=0.007694，因此后续表征实验必须使用`best_macro.pt`而不是`latest.pt`。CPU反序列化、全模型张量finite检查、300轮全指标finite检查和manifest hash一致性均通过。详细结果已更新至`research/2026-08-03_five_task_tacvae_retraining.md`。
- 按用户要求使用既有 qpos-conditioned foresight 结果重画未来触觉可视化。新增三任务 held-out 多步预测图，使用 v8j Board/Vase/Card episode，统一展示 Current、GT/Pred `H=1/4/8/12/16` 和完整16步 marker L2；选中样例16步平均误差为 Board 0.789 px、Vase 0.173 px、Card 0.150 px。
- 新增 v8j 黑板 qpos-only guidance 可视化：完全不读取 `actions/joint_abs` 或 DP action，直接将 `qpos[t+1:t+16]` 作为 candidate joint trajectory，经 `qpos -> foresight -> predicted tactile -> scorer` 做受限优化。Contact/Stable/Completion 三时刻 margin 分别提升 `+1.534/+3.522/+1.175`；图中完整展示引导前后 `H=1/4/8/12/16` 触觉后果。评分器 action branch 在 before/after 中固定，避免 action-only shortcut。脚本、PNG/PDF、CSV/JSON/NPZ、图注和详细边界记录分别保存于 `paper/`、`paper/figures/` 与 `research/2026-08-03_qpos_multistep_visualization.md`。
- 按用户许可关闭后台 RDP 部署 server：终止 tmux `rdp_board_server_smooth`，端口 `8765` 已释放。保留非 server 的五任务 TacVAE smoke 训练进程。

2026-08-04
- 继续完善 qpos-conditioned 多任务未来触觉可视化。修复 `paper/plot_foretac_multitask_qpos_preview.py` 对已卸载 `czy_data22` 绝对路径的依赖：Vase 改为兼容 EXTERNAL_USB 中同一 260630 held-out episode，Card 改为兼容 SANDISK ELE 中同一 260707 held-out episode；held-out 判定仍严格使用 checkpoint 保存的原始验证集路径。
- 从多任务可视化的数据加载和条件构造中彻底移除 `actions/joint_abs`，当前契约明确为 `qpos[t+1:t+16] -> foresight -> tactile prediction`。脚本新增 CUDA 强制检查，GPU 不可用时直接报错，禁止静默退回 CPU。三个 episode 的 qpos、marker、global RGB 和 wrist RGB 时间轴已核验一致，分别为 653、501、425 帧；py_compile 与 diff check 通过。
- 重新加载 `nvidia_uvm` 后在宿主环境恢复 RTX 4090 CUDA，并完成正式 GPU 重算，未使用 CPU 替代。多任务结果稳定复现为 Board `t=178 / 0.789 px`、Vase `t=382 / 0.173 px`、Card `t=334 / 0.150 px`；黑板三阶段 guidance margin 分别提升 `+1.534/+3.522/+1.175`，每阶段保存完整 16 步引导前后未来触觉序列。最终图将任务名与时间拆为两行，并采用共享 Current 列标题，视觉复查无文字遮挡。两个 qpos 脚本固定随机种子、关闭 TF32、启用 deterministic algorithms，并设置 CuBLAS workspace；连续两次 GPU 运行的 CSV/JSON/NPZ SHA256 完全一致。

2026-08-05
- 按用户要求生成独立展示版 t-SNE：无接触只保留夯薯片 Chip 的31个窗口，其他接触状态保留原采样；对原始 p=50 坐标做无监督 KMeans 岛屿重心收缩（factor=0.82，28个岛屿），只用于素材排版，不用于几何或聚类结论。展示版共2531点，图例下无点中心遮挡，已输出 PNG/PDF/NPZ/JSON。
- 按用户反馈调整 TacVAE 接触状态单图：上一版 perplexity=150 虽然画布占用率更高，但把不同颜色的条件小岛拉得更开。最终恢复原审计的 perplexity=50 缓存坐标，保留单面板紧凑版布局、右下角双栏图例和0点中心遮挡。脚本默认输出缓存 p=50 版，`--reembed` 仅用于敏感性对照。
- 按用户反馈进一步改善 TacVAE 接触状态单图的紧凑度。固定同一3,000个 held-out 45D特征、PCA初始化和seed42，将t-SNE设为perplexity=150、early exaggeration=6、2,000轮；30x30网格占用率从旧图28.7%提升至37.6%，trustworthiness@10=0.9907。未按标签移动单点或簇。将接触状态和任务说明合并为右下角双栏内嵌图例，程序检查图例下散点中心数为0。新增最终坐标 `paper/figures/tacvae_contact_quality_task_tsne.npz`，同步更新PNG/PDF/JSON。
- 将五任务 TacVAE 六类接触质量 proxy t-SNE 重绘为单面板论文版：颜色表示接触状态，形状表示 Board/Vase/Card/Chip/Socket 任务。新脚本 `TFAC_V5/plot_contact_quality_task_tsne.py` 直接复用旧审计的3,000个 held-out 样本和缓存 t-SNE 坐标，不重采样、不重嵌入、不删点；仅通过单图比例、边距、点大小和独立双图例提高紧凑度与可读性。输出 `paper/figures/tacvae_contact_quality_task_tsne.{png,pdf,json}`，2项单测、py_compile和视觉检查通过。任务形状显示低压/过压/振荡均只有Board，卡滞/反弹只有Card和Socket；该图不是完全交叉的五任务六状态证据。
- 按用户决定改用项目网页现有的“真实操作视频 + 实时触觉前瞻可视化”素材制作论文图。逐一检查 Board/Vase/Card/Chip 的 real/viz 同步视频联系表，并按动作语义选择 Approach、Contact、Interaction、Completion 四个对应时刻；选定时间分别为 Board `2.8/5.7/19.8/28.3 s`、Vase `1.9/3.8/11.4/19.0 s`、Card `1.8/3.7/6.4/9.1 s`、Chip `2.6/5.1/9.0/12.8 s`。
- 新增 `scripts/build_realtime_foretac_montage.py`，从网页 MP4 中按完全相同时间戳抽取 real/viz 帧。首版四任务上下双行布局经用户指出不适合论文后，改为紧凑主文版：裁掉网页原生任务标题和 `4x speed` 角标，删除弱信息的 Approach 与 Chip 行，仅保留 Board/Vase/Card 的 Contact、Interaction、Completion；每个阶段左侧实机、右侧在线 foresight，同一任务仅占一行。输出为 `paper/figures/foretac_realtime_correspondence.{png,pdf,json}`，正式英文图注保存为 `paper/figures/foretac_realtime_correspondence_caption.md`。最终视觉复查无加速标志、标题遮挡或大块无效留白。本轮按用户要求不 commit、不 push。
- 使用新五任务TacVAE `best_macro.pt`@epoch296完成untouched test split的t-SNE表征审计。新增`TFAC_V5/visualize_five_task_tacvae_tsne.py`及3个采样协议单测；阶段主图使用2,400个窗口，严格平衡5任务x3阶段；六状态proxy图使用3,000个窗口，每类500。阶段45D silhouette=-0.0183、任务silhouette=0.1016；episode-grouped阶段/任务balanced accuracy=44.23%/91.56%。45D 10-NN同任务/同source/同episode纯度=93.96%/78.49%/51.51%，说明t-SNE小岛主要由任务域、采集source和相邻episode窗口形成，不是全局统一的物理接触阶段块。六状态proxy分组探针为68.19%，但状态-任务Cramer's V=0.4596，必须保留弱标签与任务混杂限制。图、特征、固定样本manifest与summary保存至`outputs/five_task_tacvae_tsne_20260805/`，完整结论见`research/2026-08-05_five_task_tacvae_tsne.md`。
- 参考Xue等(2025) RDP论文Fig.3的表达方式，从本地擦黑板HDF5制作触觉标志点位移素材。新增`scripts/visualize_board_marker_displacement.py`，在原始左触觉RGB图上叠加黄色`9x9`位移箭头；每条episode以无接触frame50为参考作差，去除sensor静态offset，箭头保留真实像素尺度未放大。生成正常擦拭Approach/Initial contact/Stable wiping/Peak shear四阶段组图，以及Insufficient/Stable/Excessive/Oscillation四模式对照图，同时导出8张约1600x1600无标题独立PNG。低压/稳定/过压平均位移为0.40/2.89/7.87 px；振荡逐帧变化1.931 px，显著高于稳定擦拭0.073 px。素材保存至`paper/figures/board_marker_displacement/`，方法和选帧见`research/2026-08-05_board_marker_displacement_visualization.md`。
- 重绘 ForeTac v3 首页 Fig. 1 动机图。新版不再重复 Fig. 2 的模块架构，而以真实黑板交互为共同起点，使用既有 v8j qpos-conditioned 结果展示未引导与 ForeTac 引导后在 `H=1/8/16` 的多步触觉未来，并报告稳定交互时质量 margin 提升 `+3.52`；底部补充 Board/Vase/Card/Socket 四类真实任务。新增可复现脚本 `scripts/draw_foretac_fig1_v3.py`，输出 `paper/figures/fig1_v3.{png,pdf}`，并同步到 `/home/chenshuai/Project/vtm-manuscript/foretac_v3/figures/`。`main.tex` 已切换到 `fig1_v3.pdf` 并更新匹配的新图注，Tectonic 编译成功，首页单栏实排无重叠或越界。本轮未 commit、未 push。
- 为 ForeTac Fig. 2 绘制无文字、无公式的方法总览草图。新增 `scripts/draw_foretac_method_overview_v2.py`，输出 `paper/figures/foretac_method_overview_v2_label_free.{png,pdf}` 并同步到 v3 论文 figures 目录。构图分为上方离线 TacVAE/Foresight/Scorer 学习与下方在线推理两层；在线前向链路明确为当前观测、冻结生成式策略、动作条件化 Foresight、多步触觉后果、接触质量评价和有界动作修正，粗绿色回路表示评分梯度经过预测链返回当前动作样本，灰色回路表示 receding-horizon 重观测。使用真实黑板帧和既有 v8j marker 场，原 `foretac_overview.png` 未替换。本轮未 commit、未 push。
- 根据用户纠正，将 `/home/chenshuai/Desktop/图片6（第 15 个复件）.tif` 作为 Fig. 1 而非 Fig. 2 的视觉基准，新增 `scripts/draw_foretac_fig1_illustrated_v4.py` 和 `fig1_v4_illustrated_guidance_label_free.{png,pdf}`。保留人类触觉感知与三种未来接触后果的生动叙事；Guide 区结合 TouchGuide 的动作分布 steering 与 DynaGuide 的逐步去噪力方向，区分橙色基础策略、绿色预测接触引导和灰色未选模式；用真实 guided marker 场替换旧时域文字区域，并将底部 Board/Vase/Card/Chip/Socket 全部改为三阶段真实任务帧。图内不含新增文字或公式，当前论文 Fig. 1 引用未替换，本轮未 commit、未 push。
- 按用户指定的原始视频重新提取 Fig. 1 底部真机预览：Board 来自 `/home/chenshuai/Desktop/小米办公Pro20260803-100043.qt`，Card 来自 `/home/chenshuai/Desktop/小米办公Pro20260803-100108.qt`。两个任务均使用开始、接触执行、完成三帧，替换后已重新生成并视觉检查 `fig1_v4_illustrated_guidance_label_free.{png,pdf}`；无播放器控件或加速标志，未替换论文当前 Fig. 1 引用，本轮未 commit、未 push。
- 只重画 Fig. 1 右上角的梯度引导与动作采样区，其他叙事元素和真机帧保持不变。新设计结合 TouchGuide 的动作分布 steering 和 DynaGuide 的双梯度去噪表达：橙色箭头是 `Policy denoising`，绿色箭头是 `Contact guidance`，深色节点是逐步修正的动作样本，最终进入同心绿色安全动作分布并得到 `Guided action`。去除旧版难以解释的多模态大圆和灰色分支，必要文字为 `Guided action sampling`、`Noisy action`、`Policy denoising`、`Contact guidance` 和 `Guided action`。输出 `paper/figures/fig1_v5_illustrated_guidance.{png,pdf}`，当前论文引用未替换，本轮未 commit、未 push。
- 将 Fig. 1 底部五任务的 15 张真机图单独导出为 PPT 可编辑素材。新增 `scripts/export_fig1_task_preview_assets.py`，严格复用图中的源帧和 `220:203` 裁剪比例，以 `1100x1015` 高分辨率、JPEG quality 97 输出。素材目录为 `paper/figures/fig1_task_previews_15/`，文件按 Board/Vase/Card/Chip/Socket 和 Start/Contact/Completion 统一编号 01--15；本轮未 commit、未 push。
- 重画 ForeTac Fig. 2 方法总览图，新增可复现矢量绘图脚本 `scripts/draw_foretac_method_overview_v3.py`。图采用上下两层结构：上层 Offline Learning 分别展示 TacVAE 接触表征、action-conditioned tactile foresight 和正/负 key-contact pair 训练的 contact-quality scorer；下层 Online Inference 展示当前观测、冻结生成策略采样、`H=1/4/8/16` 未来触觉预测、expert-good margin 评分、trust-region 有界更新和真机执行。粗绿色回路严格表示 `score -> bounded update -> current action sample` 的预测接触梯度，不将 Guide 误画为独立策略。使用真实黑板 RGB 和既有 v8j qpos 多步 marker 结果，输出 `foretac_method_overview_v3.{png,pdf,svg}` 和英文图注。按 7.1-inch RA-L 双栏宽度进行了实际 LaTeX 缩放检查，矢量 PDF/SVG 和 300-dpi PNG 均通过；未替换 `main.tex`，本轮未 commit、未 push。

2026-08-06
- 按用户指定将 `/home/chenshuai/Desktop/图片10.jpg` 替换为 ForeTac v3 论文 Fig. 1，保留原始 `3999x2342`、330 DPI JPEG，论文素材命名为 `figures/fig1_draft_20260806.jpg`。在图片与正式英文图注之间加入黄底黑色粗体中文备注“图片最左边待改    最右边待改    文字部分待加”；备注为独立 330 DPI 无损 PNG `fig1_draft_note_zh.png`，避免引入中文 LaTeX 字体对 IEEE 模板产生副作用。同步将旧的 board-specific Fig. 1 图注替换为与新图匹配的正式概括性图注。完整 Tectonic 编译通过，论文仍为 8 页；首页截图检查确认图片、中文备注和图注无裁切、无越界。本轮未 commit、未 push。
- 2026-08-06：新增 `for_show_xiaomi/RDP部署文档_完整.md`，整理擦黑板 RDP 的本机 RTX 4090 Server + RealMan Client 完整部署流程。文档覆盖 slow LDP/fast AT 架构与20Hz时序、官方对齐和本地适配、真实 checkpoint/PCA/数据路径与 SHA256、两端环境与网络检查、文件同步、Server tmux 启停、hold-only/首次低速/正式参数三级放行、急停流程、全部 CLI 参数、观测/归一化/PCA/TCP 协议契约、安全状态机、JSONL 分析命令、已验证 latency/训练结果、未验证边界、排障和上线检查表。明确 hold-only 每帧仍发送 measured-q CANFD hold，且每轮结束都会 arm stop。审计发现 `rdp_client.py` 虽请求 `cfg.HIGH_FOLLOW=True`，本机参考 `RealmanEnv` 却未读取该字段并硬编码 `rm_movej_canfd(..., False, 0)`，因此正式运动前必须核验远端实际版本，当前不能声称高跟随已生效。当前仅擦黑板完成训练与 hold-only 联机，尚未执行模型真机运动，不能声称真机成功率；Server 未启动、8765 端口空闲。本轮按用户要求未 git、未 push。
- 2026-08-06：按用户要求恢复并封装 2026-07-31 首次 RealMan RDP 成功 hold-only 联机的精确历史版本，保存至 `for_show_xiaomi/rdp_holdonly_success_20260731/`。从 Codex 原始 session 截止北京时间10:15:13逐patch重建，Client SHA256=`cfb003c7...db8af0`与当时上传后本机/RealMan校验记录完全一致；Server SHA256=`98677dd6...70d7`，并与成功运行遗留Python3.13 pyc规范化后的code object完全相等。成功日志 `board_20260731_101343.jsonl` 前167行复核为8 warmup+159 running、13 plans，Server平均/最大0.912/1.725ms，AT平均/最大0.639/1.173ms。快照包含原始Server/Client/start/runbook/test、TCP依赖、SHA256SUMS、成功证据JSON和独立启动辅助脚本；明确该成功只验证hold-only，Server为原子plan切换，无后续blend/dynamics gate/HIGH_FOLLOW改动，不能用于声称模型运动成功。本轮未覆盖当前新版文件，未启动Server或机器人，按用户要求未git、未push。
- 2026-08-06：根据用户要求新增正常 RDP 直接执行版本：`for_show_xiaomi/rdp_client_direct.py` 在 Server `running` 状态将模型返回的7D绝对关节动作原样传入 `env.step()`，不加入6度拒绝、逐tick clip、Python关节限位、startup hold、速度/加速度/jerk投影或plan blending；新增 `start_rdp_direct_server.sh` 固定 `plan_blend_steps=0`，新增 `test_rdp_direct_online.py` 覆盖原样动作转发、warmup/plan_exhausted和原子Server契约；新增部署文档 `RDP_DIRECT_REALMAN_DEPLOYMENT.md`。代码review确认当前外部 `RealmanEnv` 仍硬编码 `rm_movej_canfd(cmd, False, 0)`，因此Direct版本如实使用20Hz低跟随，未擅自改为high-follow。13项单测、py_compile、shell语法和CLI help通过；未启动Server或真机运动，未git、未push。
- 2026-08-06：按正式交接要求统一 RDP 部署命名。将正式客户端入口定为 `for_show_xiaomi/rdp_client.py`，环境适配层定为 `rdp_env.py`，启动脚本定为 `start_rdp_server_formal.sh`，部署文档定为 `RDP_REALMAN_DEPLOYMENT_FORMAL.md`，并移除临时模式命名文件。客户端运行契约保持不变：Server 的 running action 直接传入 RealMan `env.step()`；旧动作处理版本保留为 `rdp_client_legacy.py` 供历史追溯，不参与正式部署。未启动 Server 或真机运动，未git、未push。
- 2026-08-06：将交接文件名进一步收敛为标准命名：`start_rdp_server.sh`、`RDP_REALMAN_DEPLOYMENT.md`。旧启动脚本保存为 `start_rdp_server_legacy.sh`，不参与正式运行。
- 2026-08-06：根据交接需要新增中文详细部署文档 `for_show_xiaomi/RDP部署流程_正式版.md`，覆盖系统架构、模型与输入契约、GPU/网络检查、RealMan文件同步、Server/Client启动、状态语义、日志核对、停止流程、排障和上线检查表。未启动Server或真机运动，未git、未push。

2026-08-07
- 按用户要求继续编辑 `/home/chenshuai/Desktop/画图2.pptx` 第5页。在 GUIDE 区加入可编辑矢量的基础动作采样条、后期去噪节点、接触质量局部等高线、`∇x Sψ` 梯度反馈和绿色修正动作；将三个接触结果汇聚到梯度接口，修复 `Slip` 标签换行。梯度视觉严格对应 `score → late denoising node`，质量场仅作动作空间局部概念示意，不宣称显式二维能量模型。原始文件备份为 `画图2_原始备份_20260807.pptx`，独立副本为 `画图2_第五页引导版.pptx`。经 LibreOffice 转 PDF、第五页 180 dpi 渲染、python-pptx 重开和 `unzip -t` 检查通过；8页完整，未发现遮挡或压缩包错误。可复现脚本：`scripts/edit_slide5_guidance.py`。
- 本次可复现脚本本地提交为 `dd1c0c0`。因两个远端 `tacfore` 均存在独立更新历史，未在脏工作树中 pull 或改写历史，而是在各自最新远端提交上安全 cherry-pick；已推送至 GitHub `origin/tacfore`（`f070849`）和 Xiaomi `upstream/tacfore`（`28f1051`）。
2026-08-12
- 使用已登录的 Google AI Studio / Gemini Pro 对 ForeTac Figure 2 参考图进行 AI 图像编辑。AI Studio Nano Banana Pro 因当前账号未绑定可计费 API key 返回 permission denied，遂切换至同账号 Gemini Pro 订阅网页端。生成 3168x1344 淡雅白底中间稿：清空了 RGB/机器人、marker 和预测/执行图像区域，保留 TacVAE/Foresight/Scorer/Inference 四区域、action conditioning 和返回 selected late step 的质量梯度虚线。模型仍会重建部分文字，因此明确标记为 intermediate，不作为零文字最终稿。文件保存为 `paper/figures/ForeTac_Figure2_AI_pastel_intermediate_20260812.png`，并同步到 Desktop。
- 为避免生成模型反复重建文字或改动细箭头，新增 `scripts/make_foretac_figure2_blank_template.py`，从纯手绘可编辑 v4 确定性生成零文字、零实物图、纯白底淡色 Figure 2 母版。所有 19 个图片对象替换为细灰边白色占位框，清空 47 个文本框和模块内文字，保留 177 个原生形状、86 条连线、token、latent grid、锁、action conditioning 和质量梯度反馈路径。输出 `ForeTac_Figure2_blank_pastel_editable_20260812.{pptx,pdf}` 及 PNG 预览，LibreOffice 导出和整页视觉检查通过，并同步到 Desktop。
- 为 Fig. 5 单栏和双栏版本增加自动峰值力标注。脚本从每条平滑曲线计算峰值，得到 Diffusion Policy 26.0 N、DP + Contact Observation 26.2 N、ForeTac 15.0 N；采用白底彩色边框标签、峰值圆点和带 shrinkB 间距的弧形箭头，箭头不覆盖曲线，并避开方法标题、状态条、图例和 retry 标签。同步更新图注与两份完整论文。
- 根据反馈优化 Fig. 6 视觉与论文版式：Fig. 6(b) 取消 symmetric-log，恢复线性 `[-0.1, 0.5]` 显示范围，小提琴/散点只绘制范围内样本，96.1% 仍由全部 305 次更新计算。力/评分曲线改为项目网页配色：紫色 $F_x$、橙色正向接触载荷 $F_y$、绿色 $F_z$、深蓝 quality score，阶段背景改为网页浅灰/橙/绿/紫。论文新增 `main_double_force.tex` 条件入口，生成默认紧凑单栏图5的 `ForeTac_v3_single_force.pdf` 和双栏横排图5的 `ForeTac_v3_double_force.pdf`，两者均为9页且无overfull。
- 按反馈进一步修订 Fig. 5/6：Fig. 6 将传感器负 y 方向接触载荷改为正向幅值 $-F_y$，图例仍标为 $F_y$ contact load；Fig. 6(b) 从中央 90% 改为完整展示 305 次 paired guidance updates，并使用 symmetric-log 轴覆盖 -7.223 至 1.956 logit，术语由 `Same-state refinement` 改为 `Paired guidance update`。Fig. 5 同时导出紧凑单栏版（约 5.1 in 高）和双栏横排版（约 2.15 in 高）的 PNG/PDF，论文默认使用紧凑单栏版，双栏版保留为可直接替换素材。
- 修订 ForeTac 主文 Fig. 5/6。Fig. 5 恢复为单栏三行插孔曲线，每行完整显示 Contact Force $F_z$ 和 EEF $z$，保留 R1--R5，删除峰值箭头与数值供后续人工标注。Fig. 6 删除实物阶段帧，改为一条 865-timestep 黑板执行的 $F_x/F_y/F_z$ 与 $[0,1]$ quality score 长图；横轴使用原始 `Timestep`，不再使用 `Task progress (%)`。下方使用 305 次同状态配对更新、61 次重规划事件和真实重算的 1,024 个平衡 held-out windows 展示 margin 增益、有效率和动作梯度热图。同步更新项目网页说明和素材；v3 论文 Tectonic 编译为 9 页，无 overfull box。
- 为 ForeTac Figure 2 制作薯片任务真实图像素材。新增 `scripts/export_chip_figure2_assets.py`，从本地 `260709_v8j_jiashupian/episode_1.hdf5` 按同一 frame index 导出 6 组 Global/Wrist 同步训练输入，并从项目网页源视频 `chip_real_raw.mp4` 导出 4 个执行关键帧及去除标题/倍速角标的 clean 版本。
- 导出 20 张 RDP Fig.3 风格触觉素材：在左侧触觉 RGB 上叠加同步 `9x9x2 marker_offset` 黄色位移箭头。位移以最初 20 帧均值为参考，20 张统一使用 `45x` 显示倍率，倍率与位移统计记录于 `paper/figures/figure2_chip_assets/metadata.json`；生成独立 PNG、四张 contact sheet、素材索引和 Figure 2 放置说明。
- 重要图示边界：真实观测图仅作为模型输入/真实结果，不能把观测帧标成 TacVAE reconstruction 或 Foresight prediction；预测端最终应替换为模型实际 decoder 输出。完整记录见 `research/2026-08-12_figure2_chip_visual_assets.md`。
- 审查并重构网页 Guidance Diagnostics。旧图将不同 replanning/window 的聚合值连成“guided denoising step”曲线，容易被误读为连续时间；direction-alignment panel 只有3个样本，signed-gradient heatmap还存在正负抵消。新增 `scripts/plot_board_wiping_evidence.py`，改用305个同采样状态 paired updates（61次replanning）的score-change分布和统计柱图，以及128个held-out windows的mean-absolute scaled-gradient heatmap。真实日志得到96.1% score-improvement、100% finite-gradient、100% trust-region rate；不再使用伪时间横轴。
- 生成双栏论文图 `paper/figures/board_wiping_guidance_evidence.{png,pdf}`：上部为网页对应的真实擦黑板执行四阶段、真实normal-force日志和真实离线scorer曲线，下部为same-state guidance统计。图注明确该执行轨迹不是unguided/guided paired rollout。同步生成独立网页修正版 `board_guidance_diagnostics_revised.{png,pdf}` 并更新网页Figure 5文字口径。
- 重排插孔Figure 5为横向三列双栏版，统一Diffusion Policy、DP + Contact Observation、ForeTac方法名，固定success/failure状态条位置，峰值和retry标签不再遮挡曲线。审计确认插孔曲线仍由确定性示意脚本生成而非raw trial logs，论文图注已明确标为illustrative profiles。ForeTac v3主文删除与新图重复且口径冲突的旧guidance表，插孔保持Fig.5、擦黑板为Fig.6；Tectonic编译9页，无overfull box。
- 新增 `scripts/build_foretac_figure2_ppt.py`，生成单页超宽、可编辑的 ForeTac Figure 2 PowerPoint 母版。上排为 TacVAE/Foresight/Scorer 三个离线模块，下排为局部介入 late diffusion state 的预测接触梯度引导；移除公式与损失，统一配色、锁图标、token、latent grid 和箭头语义。使用真实薯片 Global/Wrist/触觉输入、项目真实 TacVAE decoder crop、真实 held-out Board H-step Foresight prediction 和网页执行关键帧。PPT 已经 LibreOffice 重开、PDF 导出和 180 dpi 渲染检查，完整边界记录见 `research/2026-08-12_foretac_figure2_editable_ppt.md`。
- 根据用户反馈新增更贴近参考图的 v2 直接描摹版：恢复原图浅灰白四区底板、TacVAE 紧凑横向主链和原始三上一本比例，不覆盖 v1；第 2 页嵌入用户提供的参考图，便于在 PowerPoint 中逐对象对照调整。所有非照片元素仍为 PowerPoint 原生可编辑形状。
- 根据“复杂视觉元素不必全部可编辑”的反馈新增 Figure 2 hybrid v3。该版本保留参考图中完整的曲线箭头、梯度反馈回路、token 流和版面比例，以避免 PowerPoint 简单连接线导致的视觉退化；局部替换为项目真实素材，包括薯片 Global/Wrist/marker 输入、真实 TacVAE reconstruction、真实 held-out Board 多步 Foresight prediction、接触质量正负触觉样本和真实执行帧。接触质量示意明确为一个稳定正样本与四个失败负样本，关键标题和样本标签保留为 PPT 可编辑文本。输出 `paper/figures/ForeTac_Figure2_hybrid_v3_20260812.{pptx,pdf}` 及 PNG 预览；PPT 第2页保留原始参考图用于逐页对照。经 LibreOffice PDF 导出与 150 dpi 整页渲染检查通过。需要注意：当前图中的 RGB 路径表示通用架构，现有正式 Board action-conditioned checkpoint 实际为 marker-only；论文定稿前需按最终训练模型决定是否保留 RGB 路径。
- 用户指出 hybrid v3 采用原图底图覆盖，不符合重新手绘要求；因此新增 `scripts/build_foretac_figure2_redrawn_ppt.py` 并从纯白空白单页完整重画 Figure 2 v4。该版本不嵌入参考原图、不使用局部底图覆盖：TacVAE、Foresight、Scorer、token、latent、锁、箭头、质量梯度和 diffusion timeline 均为 PowerPoint 原生可编辑对象；仅真实 RGB、触觉、TacVAE reconstruction、held-out Foresight prediction 与执行帧为图片。评分区使用真实触觉缩略图表达一个稳定正样本和四个负样本；下方分为紧凑预测评分支路与扩散主时间轴，橙色虚线质量梯度沿空白通道返回 selected late state，蓝色路径再进入 Denoiser，避免穿过模块或预测图。输出 `paper/figures/ForeTac_Figure2_redrawn_v4_20260812.{pptx,pdf}` 和 PNG 预览，并复制到 Desktop。通过 py_compile、PPT ZIP 完整性、LibreOffice PDF 导出与 180 dpi 视觉检查。
